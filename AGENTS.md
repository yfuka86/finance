# AGENTS.md — finance (自動トレード研究リポジトリ)

> **現在地の1枚サマリは [STATUS.md](STATUS.md)**（本番構成・未解決リスク・毎日のタスク・
> データ所在・探索の現在地）。新しいセッション/別端末はまずそれを読むこと。

クロスエージェント共通の作業規約。Claude / Codex はこれを正本として読む。

## このリポジトリの目的
自動トレード戦略の研究・バックテスト。主な構成:
- `trading/jp_intraday/` … 日本株イントラデイ（リーク安全な）ロング/ショート研究。
  - `data`(バー入出力/セッション定義) `features`(特徴量) `strategy`(重み付け)
    `backtest`(約定・コスト・指標) `model`(ridge ウォークフォワード)
    `walkforward` `universe`(サバイバーシップ安全) `collector`(J-Quants 収集)
    `experiments`/`tick_experiments`(需給OFI含む戦略実験) `reference`(TOPIX/株数)
  - `daily_gap`(オーバーナイトギャップ反転の日次検証+既存データ統合ローダ)
    `daily_model`(クロスセクションridge学習+特徴量) `strategies`(戦略レジストリ+
    ¥単元バックテスト+構築方式) `futures_context`/`us_context`(先物/US夜間)
    `dashboard`(Streamlit管理画面: 戦略選択/理想・¥1000万単元/日次明細)
- **場中フラット戦略の大前提**: 寄付き建て・引け手仕舞い（オーバーナイト保有なし）、
  **個別株のみ**（master MktNm≠プライム/スタンダード/グロース=ETF/ETN/REIT/投信を除外。
  ただし現行masterに無い＝上場廃止銘柄は個別株として残しサバイバーシップ回避）。
  ショートは制度信用貸借(MrgnNm=='貸借')のみ。単元=100株の整数ロット。学習は過去年→翌年OOS。
  - 保有区分: `STRATEGIES[k]["holding"]` = intraday(既定)/overnight(引→翌寄)/cc1(引→翌引)。
    非intradayは `ret_on_fwd`/`ret_cc_fwd`（パネル列）を `score_frame` がエイリアス。
    **スコア計算はエイリアス前に行う**（当日intraday_retをシグナルに使う戦略を壊さないため）。
    ライブ執行はintradayのみ（executorが他区分をNotImplementedErrorで拒否）。
    パネル列を追加したら `daily_model._PANEL_SCHEMA_VERSION` をインクリメント（キャッシュ無効化）。
- `trading/bybit/` … Bybit 無期限先物の戦略群・バックテスト。
- `screener/` … 日本株ファンダ・スクリーナ + ダッシュボード。
- `backtest/`, `scripts/` … 米日セクターのリードラグ等の実験・最適化スクリプト。
- `data/collectors/` … J-Quants(日足/財務/分足) と Stooq(日足) のコレクタ。

## 研究ポートフォリオ（2026-08-01以降）

- 同時に走らせる仮説ファミリーは最大4本。主仕様1・感度分析1・Kill条件1・確認期間1回まで。
- 現在の4枠は `TOPIX2026`、`BUYBACK_PRESSURE`、`CORPORATE_EVENTS`、`OTM_SKEW_VERTICAL`。
  正本は `docs/ACTIVE_RESEARCH_2026-08-01.md`。
- `ensemble_core`の有料板データによる救済はユーザー判断で行わない。FLEX/MBO購入・寄前板モデル
  再探索はアクティブ研究から除外する。
- 気配不要ML v2のフォワード開始は2026-08-03、主評価日は**2027-08-02**。それまではPnLを
  集計・表示しない。6か月点はデータ欠損・コード変更・貸借判定・システム稼働だけを監査する。

## データ源の方針
- **Yahoo Finance は使用禁止**（規約）。日本株・財務は **J-Quants API**、
  海外・指数の日足は **Stooq** を使う。
- API キーは **`.env`**（Git 管理外）に置く。`data/collectors/config.py` が
  起動時に読み込む（`JQUANTS_API_KEY`, `EDINET_API_KEY`）。ソースに鍵を書かない。
- **既存データを再利用し、同じデータを重複して収集・保存しない。** 日次バーは
  `daily_gap.load_existing_daily()` が既存ファイル（`data/cache/bars_day_*`,
  `data/jp_intraday_reference/`, `data/jp_daily_history/`）を (date,symbol) で
  重複排除して読む。年ごとキャッシュを正本にし、結合ファイルの二重保存はしない。
- J-Quants は **Premium**（20年・分足/ティックadd-on・先物/指数/需給）。収集は
  **単独実行**（並列は429でデータ欠損）。コレクタは年ごとキャッシュで再開可能。

### データ源調査の結論（2026-07-30・Web調査済み・詳細はメモリ/会話ログ）
- **寄り前気配の履歴**はJPX総研「FLEX Standard Historical」が唯一の入手経路
  （寄前10本気配・成行数量を収録明記。スポット¥4万+税/月分の買い切り。2021-05以降はpcap形式
  =自前パーサ必要。個人契約可否はJPXクライアントサービス部 tminfo@jpx.co.jp に要確認）。
  J-Quants/DataCube/国内外ベンダーに個人向け気配履歴商品は存在しない。
- **リアルタイム冗長化の第一候補=立花証券e支店API**（無料・純HTTP/JSONでLinux可・
  時価一括120銘柄/リクエスト=626銘柄が~6回・自動売買公式OK。要実測: 寄前気配可否と
  2025-07導入の電話2FAの無人運用）。楽天MS2 RSS=市況500銘柄・自動売買公式OK（Windows+Excel常駐）。
  SBI/マネックス/松井/GMOに個人向け株式APIなし。kabuの制限緩和プランなし。
  TradingViewは規約で自動収集禁止=選外。履歴全般は現行（J-Quants Premium+Stooq）維持が最善。

## 高速化（シミュレーション）
- パネルは `daily_model.load_panel_cached(...)` を使う（入力mtime指紋のディスクキャッシュ、
  cold 13.7s → **warm 0.11s**。ユニバース制約もキー化済み・直近6件のみ保持）。
- transform(lambda rolling) は禁止 → `groupby.rolling`（Cython）に統一。β/cov/varはrolling和で
  ベクトル化（ゴールデン比較で全列一致確認済み）。unit_lotのlambda集計もベクトル化済み。

## 戦略研究の確定知見（再検証不要のベースライン）
- 本命: `ensemble_core`（ml_mag_adaptive+svdn_concentrated 50/50, OOS 7.81@3bps/6.24@7bps）。
  執行は**寄成→引成のみ**。日中±バリアは約定現実性検証で**棄却**（ブリーチ足終値約定は
  実装不能な好条件。exec_overlay.py の警告参照）。遅延エントリーも全滅＝寄付き参加が本体。
- サイジング＞シグナル: |予測|比例ウェイト+適応絞込で+1.3〜2.3 Sh。IV加重はEWに劣後。
- ensemble_coreは**ドル中立のみでβ中立化なし**だが、実測で事実上βフラット
  （OOS24+: 実現β=-0.07・R²=0.001・市場上昇日+42bps/下落日+38bpsと対称。
  日々の瞬間β露出はp5-95で±0.8対元本と振れるが日次で符号が入れ替わり洗い流される）。
  明示的Σwβ=0化は不要（beta_neutral構築は成績劣後・ライブ未実装のまま維持）。
- kabu API制約: OCO/W指値なし・注文訂正なし(取消→再発注のみ)・PUSH銘柄上限50・
  **発注10件/秒**（2026-07-10 Ver5.44.0.0で5→10件に緩和・要コード確認）・
  HoldQty拘束で同一建玉に複数返済不可。
- kabu API制約・Windows本番VPSでの実測 (2026-07-29確定・すべて本番口座で検証済み):
  - **信用注文は Exchange=9(SOR) 必須**。東証直指定(1)は 100368「信用新規注文は
    抑止されております」で全拒否 (エラー表: 市場に1を指定した信用新規が該当)。
    指値・寄指(板寄せ系)とも SOR で受理→取消を確認。kabu_client は SOR 既定+東証フォールバック
  - **GET /board は照会銘柄を自動登録し上限50** (51件目から4002006)。45件ごとに
    PUT /unregister/all で回避 (無いと plan の板coverage が4%に落ちる)
  - 参照系レート制限10req/s (11件目から429)・**Session再利用必須** (無いと1req/秒)
  - kabuステーションは夜間セッション切れで**毎朝ログインが必要** → 自動ログイン設定必須
    (2026-07-29朝: 未設定で 401、entry不発の実障害)
  - タスクスケジューラ実行は stdout=cp932 → **PYTHONUTF8=1 必須** (¥/絵文字printで
    UnicodeEncodeError クラッシュの実障害)
  - 検証環境(18081)は板が全null・注文はスタブ (受理のみ・建玉生成なし)。板=本番読取+
    発注=検証の HybridKabuClient (KABU_DATA_ENV=prod) で通しリハーサルする
  - 大引けは**15:30** (2024-11-05以降、15:25からプレ・クロージング)。exit 14:55発注→
    15:30板寄せ約定・state 15:40 の根拠
  - 口座区分: 特定(4)は約諾書未確認(100203)で発注不可のため当面 LIVE_ACCOUNT_TYPE=2(一般)。
    約諾解消後に4へ戻す (一般口座の約定は確定申告で自己計算が必要)
- ML再学習は**年次で十分**（四半期/月次/3年窓は本番構成で年次に勝てず・検証済み）。
- 2026-07-28 実運用前トーナメント再確認: ライブ実行可能な全戦略×{6,8,10}銘柄/側を
  IS(前60%)選択→OOS(後40%)確認で総当たり → **ensemble_core/8維持**が結論
  （ml_mag_adaptive単体はIS首位だがOOSで逆転されDD約2倍。6/側はIS選択規律で選べず）。
  実取引可能性も4方向検証済み（ショート実在98.4%・参加率p95 0.11%・コスト損益分岐~14bps）。
  停止基準・実効コスト実測手順は live/README.md 参照。
- 本番推奨構成(R6条件付きショートキャップ後): ensemble_core ¥20M・8銘柄/側・信用2倍・EW。
  7bps・**現行本番条件（¥10億フロア＋規制銘柄ショート除外）**: OOS24+ 年率79.1%/Sh2.98/DD-18.7%。
  （旧¥5億フロア時代の 101%/Sh3.21 は条件が違うので比較に使わない。正本は verify_baseline.py）
  **OOS起点の定義に注意**: 本番換算のOOS24+は2024-01-01（暦年境界）、リサーチOOSは
  2024-08-01（IS前60%/OOS後40%分割点）。再現は `scripts/verify_baseline.py`（4項目）。
  **キャパ天井~¥100M**（¥100Mでも51%/Sh2.00。旧一律50単元キャップが唯一の減衰要因だった。
  価格規制はトリガー銘柄(全行の2.2%)のみ対象のため条件適用に変更。参加率p95<1%は~¥90-110M）。
  R6棄却: スリーブ別銘柄数変更・ML傾斜(60/40,70/30=IS過学習)・レバ水増し(2.4x=質の低いレバ)・
  適応絞込の単元移植(8/側は既に極集中)。
- R5棄却の反対パターン: ボラターゲティング(ボラ高=稼ぎ時なので逆効果)・決算日除外(ギャップは
  アルファ源)・月曜除外(ISアーティファクト)。
  ※「流動性1e9フロア」はR5当時は棄却したが、**2026-07-30に本番既定として採用**
  （理想BT基準ではSh3.25→2.98のコストだが、板取得時間と執行品質との交換）。
- 2026-07-29 モデル改善候補4本を更新データ(〜07-28)でOOS規律検証（選択≤2024のみ・
  確認2025+を1回だけ・再調整なし）→ **全滅・現行維持**:
  ①需給セクター空売り比率z特徴量(選択窓Δ+0.03で未達) ②ridge alphaネスト自動選択
  (Δ+0.004。この行数のridgeはα∈{3..100}に非感応) ③前日イントラデイのセクター相対化
  (Δ−0.01) ④demeaned×rank予測のz合成50/50: **選択窓Δ+0.28→確認窓で−0.14に反転**
  ＝選択窓過学習の実例（OOS絶対規律が正しく機能）。ml_mag_adaptive(demeaned単体)維持。
- 2026-07-29 βのモデル導入3候補もOOS規律検証で全滅（β水準特徴量=選択段階Δ+0.01で棄却・
  β調整残差ギャップ=選択+0.18→確認+0.07に半減し本番形式は−0.11・β5分位内デミーン=
  選択+0.24→確認+0.01でゼロ化）。実現βフラット実測と合わせ、**βはリスク管理としても
  αとしても導入不要**が確定。βありの再検証は不要。
- 2026-07-29 日中利確オーバーレイ検証(1分足2年・次バーopen約定+テイカー5bps割増・
  選択24-08〜25-09/確認25-10〜26-07を1回のみ): **銘柄別TP+2%ソフト監視**は確認窓ΔSh+0.43で
  基準達成だがtop3日除去で+0.18に劣化・源泉は**LONG側の寄直後急伸の平均回帰のみ**
  (SHORT側tailは確認窓で符号反転)・効果の本体はリターンでなく裾圧縮(DD-18.9%→-14.3%)。
  **ポートフォリオ日次利益ロックは棄却**(選択+0.25→確認-0.15に反転=過学習。コスト
  ゼロ仮定でも改善なし。利益は寄付き30分に集中し以降フラット＝早仕舞いに価値なし)。
  **本番は寄成→引成を維持**。TP導入の前提=ライブでF1ペーパー並走→実効スリッページ実測
  (損益分岐~24bps)→新規フォワード期間で確認。F2返済指値方式は全閾値でF1劣後(利益を
  閾値でキャップするため)。再検証不要。
- 2026-07-29 日中SL(損切り)検証(TP検証と同プロトコル・1分足): **SL単独は棄却**（確認窓
  ΔSh+0.02・¥ネット両窓負。-2%到達後の条件付きドリフトは平均≈0で窓間符号反転＝続落構造なし。
  DD圧縮はするが保険料が恒常負の「¥を食うボラ圧縮装置」。大損日は少数銘柄のSHORT大陸続落型で
  下位20日損失の18.8%は救えるが残り460日で吐き出す）。**TP2%+SL2%併用は確認窓ΔSh+0.68・
  DD-18.9%→-10.4%で基準達成だが導入保留**: SLのグロス寄与はゼロ（守17.6M≈奪17.7M）、
  寄与はTPグロス+分散圧縮のみ、発動の6割超が寄後30分の高ボラ帯でスリッページ未実測。
  導入判断はライブのシャドー監視（発注なしで仮想発動を記録）でスリッページ実測→
  新規フォワード期間でΔSh+0.2維持を確認→ユーザー承認、の順。再シミュは不要。
- 2026-07-29 非フラット戦略探索（事前登録GO基準 net Sh@10bps≥1.0 & corr(ensemble_core)<0.4）:
  ルール系反転4ファミリー（ON反転精緻化/ギャップ持続・回帰/cc1反転精緻化/需給・業種指数ON）は
  全滅（最良でもSh@10=-0.67。grossに構造があっても毎日全回転の往復20bpsを超えられない）＝
  **手作り反転×overnight/cc1は構造的に死亡・再検証不要**。夜間は反転でなく継続が強い
  （ボラ正規化反転がSh-7〜-8）のも副次発見。
- 同日 ml_overnight（ret_on_fwdデミーンのridge年次WF・確認窓Sh@10=8.70主張）は敵対的検証3点で
  **幻影と確定・棄却**: grossの96.8%が流動性上位1022銘柄の外＋41.4%がret_on_fwd汚染由来
  （下記バグ）。実装可能版（15:24シグナル×流動ユニバース）はSh@10=1.05・2026年-0.45で
  ensemble_core(OOS24+ Sh2.98@7bps・現行本番条件・キャパ~¥100M)に全面劣後。引けオークションlook-ahead自体は
  無害（ridgeのintraday_ret係数が極小）・ショート貸借比率も97%で健全、死因は流動性とデータ汚染。
- ★2026-07-30 上記の修正は**不完全だった**（R7が発見）: 暦日差<=4のガードは
  流動性フィルタで2〜4暦日ぶん抜けたケースを素通りさせ、選択窓の3.57%の行が真の翌営業日を
  指していなかった（該当行 平均+60.4bps vs 真値+16.8bps）。**schema v7 で
  「取引所カレンダー上の真の翌セッションのみ有効」に強化済み**（全銘柄の日付集合から
  営業日インデックスを作り連番+1のみ許可）。R5-J1が観測した「夜間+13.54/+17.45/-9.16bps」は
  この汚染＋分割アンカー未補正の合成物で、**生データの真値は+4.20/+5.12/+2.45bps**。
  日中脚(intraday_ret)は生データと完全一致で無汚染＝既存の場中フラット戦略群は影響なし。
- 2026-07-30 **日次データの調整基準スプライスを修正**: 正本(daily_adj_*)に2025-12-19〜
  2026-04-24の穴があり、未調整価格のスナップショット(bars_day_*)が埋めていたため、
  当該期間に分割した銘柄で境界日に×4〜×200の偽リターンが発生していた。
  ①load_existing_dailyのソース優先順位を正本優先に ②収集スクリプトのカバレッジ判定から
  bars_day_*を除外し正本として84営業日を再取得 → 2025-12-19の異常が52件→9件（他年と同水準）。
- 2026-07-30 R7（気配レス×オーバーナイト）**全4ファミリー棄却**: 夜間アノマリー直接収穫は
  真値グロス5.16bps/日 vs 必要15.67bps（執行14+信用1.67）で損益分岐1.74bps/side＝呼値半
  スプレッド7.54bpsの1/4で到達不能（※7.54は旧呼値表由来の過大値。現行表なら1.21〜6.66だが、
  下記「コスト数値の使い分け」の通り**R7の棄却理由はコストでなく約定不能性**なので結論は不変）。残存グロスも裾依存（|夜間|>5%の4.2%の建玉が寄与216%・
  ±5%winsorizeで符号反転）＝ストップ高級の寄値で約定不能。ON-ML再検証・低回転版・
  市場レベル夜間も全滅（市場全体の夜間ドリフト+5.12bps/日は実在するが往復14bpsで取れない）。
  ¥20Mでは「大型をショート」が構造的に不可（1単元>予算で28.4%のスロットが埋まらない）。
- 旧記述（schema v4 の修正について）: ret_on_fwd/ret_cc_fwd がフィルタ済み
  パネル上のshift(-1)だったため、流動性フィルタ等で翌行が抜けた銘柄は「翌日」が数週間後になり
  未調整の株式併合ジャンプ(+959%等)を跨いで偽アルファを生んでいた → 翌行との暦日差>4日を
  NaN化するガードを追加（daily_model.py・回帰テスト付き）。**修正前のovernight/cc1系の
  全バックテスト成績は無効**（実験枠2本の管理画面表示値・M1選択窓Sh3.94含む）。
- 2026-07-30 モメンタム系クロスセクション戦略をOOS規律で検証（選択窓2021-2024・
  日中実行/5日トランシェの2ファミリー8変種）→ **全滅・再検証不要**。日中系は最良でも
  net Sh-3.26でgrossから負（過去勝ち組の日中ロングは-12.4%/年で反転。効くのは負け組
  ショート側+10.4%/年のみで単体未達）。中期5日トランシェはコスト壁を4bps/日に解決しても
  gross自体が負(-0.35)＝日本株に中期モメンタムαが不在（学術的定型事実と整合）。
  建玉は流動性中心で幻影ではなくα不在。夜間継続の副次知見とは矛盾しない（本棄却は日中/中期）。
  - ★2026-08-02 **この棄却をコミット済みコードで再現・裏付け完了**
    （`scripts/experiment_jp_momentum.py` / `data/jp_momentum/summary.json`）。
    当時のスクリプトは存在しなかったので、同じ窓(2021-2024)・同じ方向(勝ち組ロング/
    負け組ショート)・標準5定義(12-1/6-1/3-1/60d skip5/20d skip1)で組み直した。
    **日中実行(本番制約・単元・8銘柄/側)は1bps/sideで net Sh −1.36〜−2.68・DD−90%超**。
    **グロス(コスト0)でも h=1/5/10/20 のほぼ全セルが負**（例: 6-1のh5で−0.456、
    60d skip5のh5で−0.365＝当時記録の「gross −0.35」とほぼ一致）。
    唯一わずかに正なのが 3-1(60日形成/21日スキップ) の h10/h20 で **gross Sh +0.21/+0.26・
    年率+1.1%/+1.3%**。ただしこれは**コスト前**で、20日保有でも往復コスト後の水準は
    基準1.0に二桁届かない。**結果を見た後にこの1セルを拾うのは禁止**（記録のみ）。
    副次: mom20_1のグロスが −0.32〜−0.77 ＝**短期リバーサルの存在**を再確認（既に棄却済み）。
    PIT: モメンタムは**流動性フィルタ前の全履歴**で計算してからマージする
    （フィルタ済みパネル上でshiftすると除去行を跨いで繋がる＝schema v7と同じ罠）。
    さらに休止・上場廃止ギャップを跨ぐ形成窓は暦日スパンで弾く。
- 2026-07-30 銘柄粒度の需給3ファミリー（週次信用残/売買内訳/空売り残高報告=大口）を
  PIT厳守（公表ラグ保守設定）+OOS規律で検証（選択窓2021-2024・intraday/5日トランシェ×各4変種）
  → **全滅・単独戦略としては棄却・再検証不要**。週次信用残は事前登録方向が有意に逆
  （IC5d−0.013, t=−4.7＝買残過熱は短期順張り側。反転仮定でもコスト未達）。空売り残高報告は
  grossから負。**売買内訳のみgross実在（翌日intraday Sh+2.9〜3.6）だが公表翌日1日でほぼ完全減衰**
  ＝日次全回転14bps/日の壁を超えられず。ens相関は全組合せ|ρ|≤0.31。
  残る余地=売買内訳フローの**ML特徴量化**（独立戦略でなく）を新窓で1回勝負のみ。
  データは data/jp_flows/（collect_jp_margin_flows.py・信用残は分割無調整に注意=スクリプト内で
  基準変更検知実装済み。パネルraw_closeは2022-2024欠損の罠あり）。
- ★**撤回済み（下記§特別気配の現実を参照）**: 以下はノイズを「iidランダム」と仮定した机上値で、
  実際の気配誤差は特別気配の離散更新に支配されるため**桁が2つ足りない**。歴史的記録として残す。
- 2026-07-30 寄前気配シグナルのノイズ耐性検証（運用制約「シグナル気配は08:50まで」対応。
  約定は板寄せ参加で常に実寄値=無問題。問題は銘柄選択のみ）: 選択ノイズσ(銘柄固有)に対する
  OOS24+劣化は σ=10bps無感応・25bpsで−1〜2%・**−10%到達はσ≈51bps（単元）・−25%はσ≈87bps**。
  共通モードの気配ズレはdemeanで完全相殺。ml側はridge係数の縮小で気配ノイズが1/13〜1/100に
  減衰し¥の62.5%が境界余裕>100bpsに集中=極めて頑健。**脆弱なのはsvdnスリーブ**
  （裾p95選択でノイズがほぼ1:1透過・σ=100bpsで単元Sh2.30→−0.37）。日次1-2銘柄の
  選択入替は構造的に正常（境界隣接の等価交換・α中立）。
  （★上記の閾値は撤回済み。実測は `scripts/analyze_quotesnap.py` の系統/ランダム分解で判断）
- 2026-07-30 気配レス戦略探索（当日寄値・寄前気配を使わないフラット戦略）: 事前指値テール
  フェード4変種（最良Sh0.77・0.32約定/日。ivol上位は指値が寄値から±24.5%乖離し約定せず、
  全銘柄に広げるとテールギャップは反転せず継続=gross負け）と前日特徴量ML 4変種
  （全netマイナス・grossでも最大1.18）が選択窓で全滅 → **NO GO確定・再検証不要**。
  **ギャップ反転αは「当日寄値のクロスセクション比較」が本体**で、前日確定データ＋
  先物ナイトでは捕捉不能（ensemble_core同窓net Sh2.95に対し−2.2以上の差）。
  追試: 指値のボラ倍率k∈{0.5..2.0}全域でも不成立（Sh−2.7〜−0.8の単調・全て負。
  深さ別の条件付きリターンは浅い域+1〜3bps=コスト14bps未満・深い域は継続で負、
  かつ絶対閾値方式は約定が市場方向に71%片寄る=ドル中立も崩れる。kの調整では救えない）。
  運用帰結: **気配取得不能日（board/PUSH障害等）は劣化代替でなくその日ノーエントリー**が正解。
  再探索は前日確定の新データ源追加時のみ・新しい未使用期間で（選択窓2021-2024は消費済み）。
- 売り制約解除実験(2026-07・敵対的検証済み): 50単元キャップ/¥10億フロア解除は¥20Mで無影響。
  貸借限定解除のみ劇的(Sh2.2→4.7)だが改善の80%が非貸借ショート由来（寄値10円級ボロ株中心・
  新規ショートの42%はティック半スプレッド>7bps・借株不能）＝**借株コストゼロの理論上限で実行不能**。
  実効レバ天井は制約に依らず**~2.3x**（ストップ高×30%の保証金拘束が律速。公称3.3xでも不達）。
  レバを上げてもSharpe一定(~2.2)＝リターンとDDが比例拡大するだけ。
  実験: `scripts/experiment_short_constraints_leverage.py`（フラグ既定は本番同等・特性保存確認済み）。
- **2026-07-30 リサーチ層の重大欠陥を修正**: 理想ブック(book_from_scores/全構築方式)に
  借株可能性フィルタが無く、貸借不可・規制銘柄のショートで成績が水増しされていた
  （¥10億パネルOOS Sh 4.93→1.94で検出）。`_short_eligible()` を全構築に適用して修正。
  **単元バックテストとライブ執行には元から入っていたため本番成績は不変**（OOS24+ Sh2.98）。
  影響はリサーチ表示のみ: リサーチOOS(24-08+) 8.77/7.14 → **5.09/3.13**（¥5億パネル）。
  過去のリサーチ層の相対比較（戦略間の優劣）は同一バイアス下なので概ね有効だが、
  絶対水準の主張は修正後の値を使うこと。R2のG4検証が発見。
- 2026-07-30 気配レス探索R2/R4（各4ファミリー・選択窓2021-24）: **全8ファミリー棄却**。
  R2=保有期間フロンティア(cc2/5/10。**本番制約付きL-Sスプレッドがh=1..20の全horizonで負**
  ＝薄めるべきグロスαが存在しない)・引け集中執行cc1(gross 3-11bps/日で片道10bpsに届かず)・
  レジーム条件付き(稼働日を減らしてもコスト率は下がらず原理的にコスト壁を越えない)・
  残差定義の別軸(β/クラスタ/PCA残差。gross最良+0.09)。
  R4=信用需給の主体分離(**分離は情報を減らす**: IC総売残-0.018 > 一般-0.012 > 制度-0.012、
  大口報告0.5%+はIC≈0＝既に織り込み済み。個人買残の逆張り仮説は符号が逆で非有意)・
  セクター集中/ローテーション・銘柄集中/ペアトレード・貸借逼迫(唯一Sh1.77だが上位1銘柄で
  寄与38.7%・直近年で消滅し幻影判定)。
  **副産物で判明した本物のα**: breakdownの需給フロー合成は gross Sh 1.92・4年全て安定
  (2.15/1.53/2.03/2.07)・1272銘柄に分散・+1日ラグでも1.45(リークなし)・ens相関0.05。
  ただし **gross 4.94bps/日（訂正: 旧記載6.16は再現不能）→ 損益分岐2.47bps/side** vs 実勢7-10bps。
- 2026-07-30 R1が上記フローαの評価を**さらに下方修正**: 「gross Sh +2.9〜3.6」は
  **本番ショート制約なしの数字**だった。制約を段階適用すると
  ¥5億フロア/制約なし +2.92,+3.66 → ¥10億フロア/制約なし +2.87,+3.30（流動性フロアは無害）
  → 貸借のみ +1.72,+1.86 → **貸借+規制除外(本番) +1.24,+1.02**。
  αは**100%ショート脚**（ロング脚Sh -0.02〜-0.20）で、無制約ショート銘柄の94%は既に貸借なのに
  **残り6%の非貸借銘柄がαの約45%**を担う＝既知の「借株不能の理論上限」と同一シグネチャ。
  純ショート・スリーブ単独も算術上 net Sh≈0.04（gross0.5で年率+18.1% vs コスト年17.6%）。
  週次信用残は**当日寄→引に対してはIC≈0の完全null**（IC5d=-0.013は5日先の話で当日には無関係）。
  **結論: 当日寄値を外すとgross年率は最良+4.9%（ensemble_coreは+69.4%）＝場中フラットαは
  実質すべてオーバーナイトギャップに宿る**。「気配レスで日次回転」はコスト構造と両立しない。
- 2026-07-30 R5でフローαの活用を追求 → **NO GO・構造的に「閉じた」**（再挑戦不要）:
  ①**寄成→引成のフラット戦略は実現ターンオーバーが2.0/日でシグナルに依らず固定**
  （寄付き100%+引け100%）＝「低回転化でコストを下げる」は原理的に不可能。従来の一律14bps/日
  課金は過大でなく実現ターンオーバー課金そのものだった。②回転を落とす唯一の手段=持ち越しは
  **オーバーナイト脚が日中αの76%を吐き出す**（intraday +4.93bps vs overnight −3.74bps）。
  ③診断で判明: この日中αは情報シグナルでなく**時間帯の季節性**（大型・低ボラ傾斜が日中プラス→
  夜間で戻す。ivolソートで−13.98/+13.54、mktcapソートで+11.12/−17.45）＝保有を伸ばしても収穫不能。
  ④集中しても1建玉あたり期待は9.63bpsで頭打ち（14bps未達）・フローαは実質ショート専用。
  ⑤インデックス需給(月末/期末/規模区分変更)・規制解除の歪みも全滅（イベント7件/年等で頻度不足）。
  脱出路は「片道2.47bps未満の執行チャネル」のみで、¥20M個人信用口座には存在しない。
- 2026-07-30 **IVレジーム連動エクスポージャ**（選択窓でens_coreがQ5高IV 28.9bps/Sh5.82 vs
  Q2低IV 1.09bps/Sh0.24と判明→事前登録3変種を確認窓2025+で1回検証）→ **棄却**。
  ΔSharpe A二値+0.010 / B線形+0.076 / C五分位−0.034（基準+0.30。ブートストラップCIは全て0を跨ぐ）。
  **高IV優位は確認窓でも生存**（Q5 44bps/Sh4.35）だが、**低IVの死角が消えた**（Q1 25.6/Sh2.66・
  Q2 14.6/Sh1.32）ため絞込が良い日も削る。corr(s(t), net)が0.02-0.05しかないのが直接原因。
  副産物: 日経225オプション収集パイプライン+自前恐怖指数3系統（NKVIF相関0.94・2019-2026）、
  真の保証金利用率は平均0.614/p95 0.684（s=1.5で0.912・11%の日が上限超過→実質上限s≈1.25）。
- 2026-07-30 R8（方向性許容×高回転×指数先物）**全4ファミリー棄却**: コスト前提は正しかった
  （往復1-3bpsで必要グロスは6.9-8.9bps/日）が、①前夜ナイト→当日日中の伝播はβ=0.072・R²=0.005で
  経路が枯れている ②ナイト単純ロングはゼロコストのグロスSharpe 0.89が天井（日次ノイズ83bpsに対し
  ドリフト+4.7bpsが小さすぎ、コスト削減では超えられない）③夜間の正ドリフトはβ=0.53の
  「半分の株式β」でα≒0。¥20Mではミニ1枚が資金の31%で刻みが粗い（マイクロ/ミニTOPIXが適切・未収集）。
- ★2026-07-30 **先物のセッション順序の誤りを修正**（R8が発見・実データで検証）:
  1レコード(取引日D)は**ナイト(D-1夕→D朝06:00)が先**で日中(08:45-15:45)が後
  （実証: 日通し始値O==EO 99.9%、MO/MCは100%欠損）。旧実装の nk_night は
  `EC_D/C_D-1` を D+1 に寄せており「D の日中リターンの符号反転」＝別物だった
  （真のオーバーナイト因子は一度も使われていなかった）。
  **修正後の真の因子は情報量が大きい**: nk_night vs 当日ギャップ corr **+0.812**・
  当日日中 +0.287（旧実装は翌日ギャップと−0.016）。dow_night も +0.687/+0.174。
  本番 ensemble_core は FUT_FEATURES を使わないため**過去の本番成績は無効化されない**が、
  「先物ナイトは効果ゼロ」という過去の結論は**誤った変数での検証**だったので再検証の価値あり
  （ただし市場レベル変数はクロスセクション予測に効きにくいのが既知。schema v8）。
- 実験プロトコル: IS(前60%)選択→OOS(後40%)確認→敵対的再計算。破ったクレームは全て崩れた
  （GBM・交互作用・バリア等）。約定モデルの保守性チェックを必ず含めること。
- **OOS絶対規律（ユーザー指示 2026-07-28・例外なし）**: **IS成績はいかなる判断・主張・
  表示・比較にも使用禁止**（引用も不可）。評価・報告は必ず選択に使っていない期間
  （OOS/フォワード）のみで行う。選択自体は「その時点までに存在するデータ」での
  ウォークフォワードとして行う（選択に使った期間の成績は評価として無効）。
  管理画面はOOS期間のみ表示（既定 OOS 2024-01+。IS混入の全期間表示は廃止）。
  **OOSを見て選び直した時点でそれはOOSでなくなる**（OOSでの再選択・チューニング禁止。
  やり直しは新しい未使用期間が必要）。停止基準等のリスク分位はデータ全量から取って
  よい（保守側であり成績の主張ではない）。

- 2026-07-31 **未使用需給データ4系統をMLに投入 → 増分あり（選択窓 Sh1.30→1.43）**。
  `trading/jp_intraday/extra_features.py`（週次信用残・空売り残高報告・増担保規制・
  業種別空売り比率。すべて営業日ベースのPITシフト・保守側ラグ）。
  選択窓2018-2024・本番制約・1bps/side: A現行 Sh1.30/DD−40.5% → **+4系統 Sh1.43/DD−36.8%**
  （個別: +増担保 1.26 / +空売り残高 1.36 / +業種空売り比率 1.24）。
  ★**`xt_alert_flag`(増担保・日々公表)は単体IC −0.0405・t=−13.46 と極端に強いのにMLでは
  ほぼ効かない**（1.30→1.26）。**αが規制銘柄＝ショート禁止側にあるため**で、
  「非貸借銘柄がαの45%」と同じシグネチャ。単体ICの強さに騙されないこと。
- 2026-07-31 **DD対策は全滅（タダ飯なし）**。上記v2(DD−36.8%)に対し選択窓で設計:
  ボラターゲット(目標15-40%・前日までの実現ボラでレバ調整)は **Sharpe 1.43→1.28・
  Calmar 1.29→1.06-1.18 で逆効果**（AGENTSの「ボラ高=稼ぎ時」がここでも成立）。
  risk_parity は DD −31.0% に下がるが Sharpe 1.19・Calmar 1.15 で劣後。
  dollar_neutral/sector_neutral は Sh1.14。**DDを下げるとSharpeも同率で下がる**。
  `magnitude` 維持が結論。
- ★2026-07-31 **気配不要ML v2 を凍結・事前登録**（`docs/PREREGISTER_quotefree_ml_v2.md`）。
  **確認窓2025-01+ は v1 で消費済み**なので、この構成は**新しい未使用期間でしか判定できない**。
  評価は 2026-08-01 以降が最低6ヶ月（できれば12ヶ月）貯まってから**1回だけ**。
  期待値の見積り: v1が選択1.30→確認0.87(−33%)だったので v2は 1.43×0.67 ≒ **0.96**＝
  基準1.0をわずかに下回る見込み（推定であって判定ではない）。
- ★**ML実験での欠損処理の罠**（2026-07-31に踏んだ）: 疎な特徴量を足すとき
  `dropna(subset=全特徴量)` を `dropna(subset=[target])` に緩めると、**流動性境界の
  断続銘柄が混入して成績が壊れる**（同一構成が Sh1.30 → −0.04・DD−81.5% に激変）。
  **中核特徴量は必須のまま・疎な追加特徴量のみ0埋め**が正しい。
- 2026-07-31 **気配不要ML（フロー＋前日価格の統合ridge）→ 確認窓で基準未達・棄却**。
  ただし**これまでの気配レス探索で最良**で、シグナル自体は確認窓で強まってさえいる。
  構成: Ridge(α=30)年次WF・特徴量=BASE_FEATURES−当日寄値系＋FLOW_FEATURES＋prev_value・
  magnitude・8銘柄/側・¥20M・信用2倍・¥10億フロア。事前登録基準 1.0bps/side で net Sh≥1.0。

  | 窓 | IC | t | 0bps | **1.0bps** | 2.0bps |
  |---|---|---|---|---|---|
  | 選択窓 2018-2024 | 0.0289 | 9.03 | Sh1.59 | **Sh1.30** | Sh1.02 |
  | **確認窓 2025-01+** | **0.0412** | **5.77** | Sh1.09 | **Sh0.87** | Sh0.64 |

  **判定: 0.87 < 1.0 で不合格**（確認窓を見た後の再調整はしていない＝この窓は消費済み）。
  年率は1bpsで34.5%あるがDD−32.9%が重く、Sharpeが1に届かない。
  **フローのML特徴量化には実効がある**（フロー抜きだと選択窓 Sh0.90 → 入れて1.30＝+0.40）。
  これはAGENTSが「残る余地」としていた仮説の**肯定的な検証**だが、水準が足りなかった。
  ※「当日寄値を外すとgross年率は最良+4.9%」（R1）とは矛盾する水準（確認窓グロス43.6%）。
  R1は個別ファミリーの話でML統合＋フローは含んでいなかったため。
- ★2026-07-31 **`vol20` は PIT 違反（予測特徴量に使うな）**。shift なしの20日ローリング
  標準偏差で**当日の終値リターンを含む**。気配不要MLに足したら選択窓 Sh1.30→**2.75**に
  跳ねてリークと判明（年率42%→116%）。**本番の BASE_FEATURES は無傷**（元から入っていない）。
  予測に使ってよいボラは **shift=1 の `ivol`**。退行テスト `tests/test_pit_features.py`。
- 2026-07-31 **前日分足マイクロストラクチャ 7特徴量 → 全滅**（`micro_features.py`・
  引け/寄付き板寄せの出来高シェア・前後場スプレッド・終盤ドリフト・VWAP乖離・出来高集中度・
  分足実現ボラ）。選択窓2024-08〜2025-09で最良が mic_am_pm_spread の t=−2.04、他は全て|t|<1.3。
  **日足に現れない板の"質"に横断面αは無い**。再検証不要。
- 2026-07-31 **資本を¥20M→¥50Mに緩めてもフローαは改善しない**（Sh0.58→0.42）。
  αが**信号のテールに集中**しており、分散を広げると薄まる（8→30銘柄/側でSh改善だが
  50銘柄以上で悪化）。単元制約ではなかった。**資本増は解にならない**。
- 2026-07-31 フローαの年別IC（選択窓）: 2018 +0.0119 / 2019 +0.0125 / 2020 +0.0180 /
  2021 +0.0038 / 2022 +0.0027 / **2023 +0.0243 / 2024 +0.0177**。**減衰していない**
  （弱いのは2021-22でレジーム依存）。
- 2026-07-31 **中期・個別残差ML L/Sを実行可能条件で検証 → NO-GO**。
  よしそ氏の「個別リスク中心L/S」とUKI氏の公開構成を参考に、PIT財務8＋価格8＋需給13、
  Ridge年次WF、10営業日保有/5日リバランス、33業種中立で固定。さらにTOPIX500、¥20M、
  100株単元、翌朝のPIT貸借＋規制再判定、実ターンオーバー1bps、信用金利4.2%を適用した。
  2024+診断は **gross年率1.27%/Sh0.19 → net年率−1.32%/Sh−0.20/DD−11.9%**、
  2026年net−7.2%。平均42銘柄/トランシェ、L24.95%/S24.93%、最大ネット偏り0.43%で
  単元化による片張りが死因ではなく、**実際に売れる大型株集合ではグロスαが無い**。
  分数ウェイト・広いユニバースの見かけ（gross Sh1.08）は実行制約で消えた。証券会社固有の
  過去在庫は取得不能なので、ライブではAPI売建可能数量の再確認が別途必須。再調整は禁止。
  `scripts/experiment_medium_residual_ml.py` / `docs/PREREGISTER_medium_residual_ml_ls.md`。
- 2026-07-31 **UKI型のTOPIX500階層残差LASSOを実行可能条件で検証 → NO-GO**。
  D日引け後のPIT特徴量29本からD+1寄→引を予測し、年次WF、100株単元、¥20M、信用2倍、
  当日貸借＋規制除外、板寄せ往復2bpsで固定。単元数は未知の翌日寄値でなく前日終値で決定。
  OOS24+は **gross年率13.08%/Sh1.13 → net年率6.95%/Sh0.60/DD−8.2%**。
  2024年モデルは検証MSE選択で係数が全て0となり無取引、
  各年プラス条件にも未達。結果後のalpha・特徴量・銘柄数の再調整は禁止。
  `scripts/experiment_topix500_hierarchical_lasso.py` /
  `docs/PREREGISTER_topix500_hierarchical_lasso.md`。
- 2026-07-31 **動的入替close-to-close階層LASSO → グロスαはあるが実取引NO-GO**。
  D引け後に計算→D+1引成で入替→D+2引けまで保有（1日遅延）、新規rank10%・既存は20%まで
  維持、40銘柄/側、100株単元、売建可否を毎日再判定した。OOS24+は **gross年率15.67%/
  Sh1.18 → 売買0.5bps/実turnover＋売建金利4.2%後 net年率8.90%/Sh0.67/DD−15.2%**。
  前日銘柄の平均55.3%は残るが、入替＋予測強度リサイズでturnoverは2.04/日。売買コストを
  ゼロにしても金利後の概算Shは0.86で基準1.0未満なので、バッファ調整では救えない。
  2024年net Sh0.07、2025年0.88、2026年1.12で各年安定性にも未達。再調整禁止。
  `scripts/experiment_dynamic_cc_lasso.py` / `docs/PREREGISTER_dynamic_cc_lasso.md`。
- 2026-07-31 **schema v8修正後の真の先物ナイト交互作用3仮説 → 全NO-GO**。
  D-1引け個別特徴×D朝06:00確定先物を使う気配不要の寄成→引成。TOPIX500、前日終値単元、
  ¥20M、貸借/規制、往復2bpsを適用。①beta×NK/TOPIX/Dowは **gross Sh1.31/net Sh0.77**で
  ベース比+0.17（基準+0.20未達）、2024無取引・2025 Sh0.65・2026 Sh1.41。情報はありそうだが
  絶対Sh1にも届かない。②ivol/amihud×|NK|はnet Sh0.64（増分+0.04）で無効、
  ③ret1/mom5×NKはgross Sh−0.08/net−0.65で逆効果。結果後の合成・再調整は禁止。
  `scripts/experiment_futures_night_interactions.py` /
  `docs/PREREGISTER_futures_night_interactions.md`。
- 2026-08-01 **個別株ショートを使わないロング＋ミニTOPIXヘッジ → NO-GOかつ運用対象外**。
  凍結済み階層LASSO上位40銘柄を現物ロング（¥20M、前日終値で100株単元）し、D+1寄→引。
  ロング単体はOOS24+ **年率−0.22%/Sh−0.03/DD−12.0%・実現TOPIXβ+0.33**でグロス源泉が無い。
  JPX公式のミニTOPIX（指数×¥1,000）をβnotionalに四捨五入してショートするproxy版も
  **年率−1.09%/Sh−0.16/DD−14.1%・β−0.23**（平均3.34枚）で悪化。先物の刻みで過剰ヘッジに
  なり、β基準|β|<0.2にも未達。個別ショートを指数ショートで置換してもαは復元しない。
  **個人では株式と先物の損益通算ができないため、株式戦略に先物ヘッジを混ぜる構成は実運用候補から除外**。
  日本株戦略は現物・信用株式の税区分内で完結させる。一方、**先物・オプションだけで損益が
  完結する独立戦略は探索対象**とし、株式とは損益・資金・リスク評価を分離する。
  `scripts/experiment_long_topix_hedged.py` / `docs/PREREGISTER_long_topix_hedged.md`。
- 2026-08-01 **日経225 ATMストラドル IV−RV乖離（先物・OP単体枠）→ NO-GO**。
  週初、20-45DTE、出来高あり/OI≥100のATM put+callを、IV−前日RV20の過去252日zで
  z≥+0.5売り/z≤−0.5買い、5営業日保有。OOS24+は36取引、gross **Sh0.28/DD−144%**。
  2024 Sh1.04→2025 Sh−0.94→2026 Sh0.08と不安定。2025-03-31のショート1回が
  premium比−144%で単独破綻し、裸ショートボラの有限利益・無限損失構造が顕在化した。
  bid/ask未収録以前にグロスとリスクで失格。閾値・DTE・保有期間の再調整は禁止。
  `scripts/experiment_option_iv_rv_straddle.py` / `docs/PREREGISTER_option_iv_rv_straddle.md`。
- 2026-08-01 **TOPIX 2026年10月見直しを完全フォワード事前登録**。
  基準日8月最終営業日、公表10月第5営業日、実施10月最終営業日。継続は回転率0.14/累積FFW
  時価97%、追加は0.20/96%を混同しない。8月の日次価格・売買代金・株式数・公式FFWを保存し、
  8/31に入力hashと候補を凍結する。**手元には公式FFW絶対値がまだ無いため、現TOPIX weightから
  逆算した疑似値で売買判定しない**。`scripts/topix_2026_forward.py` は日次raw+SHA256+commitを
  append-only保存し、FFW未取得警告をmanifestへ固定する。
- 2026-08-01 **企業行動PITイベント基盤を実装**。`event_id/security_code/event_family/source/
  source_published_at/first_received_at/effective_at/revision_no/document_hash/state/terms_json/
  first_tradable_at` を共通schemaとし、訂正はrevision追加、既存revisionの上書きを拒否する。
  時刻はtimezone-aware必須、受信前の取引可能時刻も拒否。`trading/jp_intraday/event_store.py`。
- 2026-08-01 **自社株買い実行圧力を事前登録・ロジック実装、正式データ待ち**。
  TDnet決議/状況/完了、EDINET日次実績、ToSTNeT-3を分離し、remaining shares/yenの小さい方から
  `remaining_pressure`、線形計画比から`pace_surprise`を計算する。仮説は実行加速・実行不足・
  完了クリフの3状態だが1ファミリーとして数える。手元finsには必要項目が無い。公式構造化履歴は
  TDnet 2013+、EDINET 2019+、ToSTNeT-3 2008+（J-Quants Pro仕様）。個人向けTDnet add-onは
  原文5年で別物。正式exportか原文抽出を得るまで分布・閾値・PnLを見ず、売買ルールを仮置きしない。
  `trading/jp_intraday/buyback_pressure.py` / `docs/PREREGISTER_BUYBACK_PRESSURE.md`。
- 2026-08-01 **バリュー株イベントドリブンをCORPORATE_EVENTS枠の主戦略として事前登録**。
  月次バリュー単体は既に確認窓Sh0.72で不足しているため、低PBRの再検証はしない。イベント前に
  固定したPBR/earnings・FCF yield/net cash/ROEと、①資本解放（買戻し実行・消却・増配・持合縮減）
  ②支配変化（重要提案目的・連続買増し・TOB/MBO）③資産解放（非中核売却・撤退・再編）の状態を
  組み合わせる。主仕様long-only、レバなし、1案件3%、最大10案件、20～120日の状態保有。
  日次Sharpeでなく案件中央値・cluster CI・ES・利益集中で判定。正式PIT履歴取得まではDATA BLOCKED。
  `docs/PREREGISTER_VALUE_EVENT_UNLOCK.md`。
- 2026-07-31 **売買内訳フローの再評価（気配前提棄却後・最初の執行可能候補）→ 棄却**。
  板寄せコスト訂正(7bps→1.0bps/side)で損益分岐を跨いだ可能性があったので再測定した。
  PIT安全な特徴量を新規実装（`trading/jp_intraday/flow_features.py`・営業日ベースのlag=2既定）。
  - **αは実在する**: `flow_close_z`(信用返済の方向=踏み上げ/投げ) の当日日中ICは
    **+0.0130 (t=6.62)**、`flow_turn_z`(フロー総量)と合成したV2で選択窓グロス **Sharpe 1.23**。
  - **しかしコストに届かない**（選択窓・30銘柄/側・本番条件・¥20M）:
    0bps Sh1.23 → **0.5bps 0.90 → 1.0bps 0.58 → 1.5bps 0.25 → 2.0bps −0.08**。
    板寄せ実測(寄1.5/引0.5=平均1.0bps/side)で **Sh0.58**＝事前登録基準 net Sh≥1.0 に
    **選択窓の時点で未達**。よって**確認窓(2025+)は消費していない**（未使用のまま温存）。
  - 分散度は効かない（8→30銘柄/側でSh0.46→0.58だが年率は11.3%→6.7%に低下。¥20Mでは
    30銘柄/側でも実建玉54本が上限＝単元制約で分散しきれない）。
  - ★**「1日で減衰」は正しかった**（AGENTSの旧記述を実測で追認）。ただし
    **h期先ICは持続して見える**（h=0..9で+0.013→+0.003が全てt>1.7）ので注意。
    これは**シグナルの自己相関によるアーティファクト**で、実際に保有した場合の
    **累積リターンに対するICは減衰する**（cc1日 0.0094 → 3日 0.0037 → 5日 0.0015 →
    10日 −0.0002）。**保有期間の判断にh期先ICを使ってはいけない**。
  - 副次: 夜間脚はこの信号では**反転しない**（引→翌寄 IC +0.0050, t=2.36）。
    ★**2026-08-11 訂正: この夜間ICは現行パネル（schema v11）で再現しない**。実測は
    **−0.0031/t=−1.82**（lag=0/1/3や¥5億フロアでも全て負）。旧値は隣接ガード修正前
    （再参入行+128bpsの系統プラス汚染）の未コミットハーネス由来。日中IC+0.0130は完全再現
    ＝**フローαは日中専用で、夜間脚はむしろ弱い逆符号**。
    R5の「オーバーナイトが日中αの76%を吐き出す」は時間帯季節性αの話でフローαには当たらない。
    ただしcc1(IC 0.0094)は日中(0.0130)に劣後するので、執行は寄成→引成が正しい。
  - ★**実装上の罠**: 売買内訳は**会計恒等式で買い代金≡売り代金**（実測100.00%）。
    `(buy-sell)/total` は定義上つねに0で特徴量にならない。情報はネットでなく**構成比**にある
    （退行テスト `test_buy_equals_sell_so_net_is_not_a_feature`）。
- ★2026-08-01 **既存Premium契約内に未使用の10年超データがあった**:
  `/v2/edinet/major-shareholders` = 有価証券報告書「大株主の状況」上位10名。
  **2016-2026・44,382件・4,589銘柄・提出日時(SubDate/SubTime)付き＝PIT安全・追加費用ゼロ**。
  `DATA_SOURCE_FRONTIER` が外部に探していた「10年以上・一次提供者・PIT可能」を、
  **契約済みAPIが既に満たしていた**。新データ源を外に探す前に既存プランの全エンドポイントを
  実キーで列挙すること（仕様ページの記載漏れがある）。
  収集 `scripts/collect_edinet_major_shareholders.py`（提出日単位・append-only・日次SHA-256）、
  特徴量 `trading/jp_intraday/ownership_features.py`、正本 `data/jp_ownership/`。
  - PIT時刻は**提出日**。決算期末 `PerEn` は約3か月前で未公開なので使わない。
  - 前年比は `period_end` 差が**300〜450日**の連続年度のみ（年度飛びを1年の変化と誤認しない）。
  - **信託口・マスタートラスト・カストディ等のプール型名義は「浮動株」**であって固定株ではない。
    保有者名で除外しないと固定株比率が壊れる（全保有者行の25.4%が該当）。
  - 銘柄内YoYの固定株比率は平均 **−0.39pp/年**で2017-2025が全年マイナス＝持合い解消は実在。
    ただし**断面平均は2016年0.431→2023年0.454と逆に上昇**する（創業者保有の高い新規上場の
    流入）。**断面水準とパネル内差分を取り違えないこと**。
- 2026-08-01 **バリュー株イベントV3＝固定株(政策保有)縮減×低PBR → NO-GO**。
  上記の新データで CORPORATE_EVENTS の「支配変化／資本解放」ブランチを初めて検証した
  （`docs/PREREGISTER_VALUE_EVENT_V3_OWNERSHIP.md`・V1と同一の実取引条件＝PBR≤1・EPS>0・
  OP>0・売買代金≥¥10億・翌営業日寄り→60営業日引け・往復20bps・同時最大10案件・
  学習 exit<2024-01-01 / OOS event≥2024-01-01・Ridge(α=10)）。
  イベント7,300件→財務ゲート2,475件→**実取引条件を満たす65件**→OOS採用**27件**。
  結果は **案件中央値 −0.47%・勝率48.1%・最大利益案件の利益シェア56.4%・ES(5%) −16.4%**。
  平均は+2.99%だが**利益の56%が1案件**＝V1(64.7%)と同じ死因で、事前基準
  「中央値>0」「利益集中<20%」の**2つに違反しNO-GO**。結果後の再調整は禁止。
  ★**副次的に判明した本質的な壁**: 7,300イベントのうち実取引条件を通るのは**0.9%**。
  低PBR（＝小型・低流動）と¥10億フロアが**構造的にほぼ排他**で、
  「バリュー×イベント」は¥20M個人口座では**標本がそもそも積み上がらない**。
  V1(17件)・V2(0/21件)・V3(27件)と3回連続で同じ制約に当たっており、
  **CORPORATE_EVENTS枠は閾値でなく標本供給の問題**として扱うこと。
- ★★2026-08-01 **気配不要ML v2 の選択窓 Sh1.43 は committed code から再現できない**。
  この値を出したハーネスは**リポジトリに存在しない**（`scripts/` に quotefree ML の
  実験スクリプトが無い）。事前登録どおりの構成（Ridge α=30・年次WF・
  BASE−TODAY_OPEN＋FLOW＋prev_value＋EXTRA の19本・疎な特徴量のみ0埋め・日次デミーン・
  magnitude・8銘柄/側・¥20M・信用2倍・¥10億フロア・1bps/side）を再実装すると
  **単元BTで Sh0.43 / DD−62%**、理想ブック(q=0.05)で **Sh3.07 / DD−8%**。
  **記録の1.43はどちらでもない**。潰した候補: パネル流動性フロア(5e8→0.22 / 1e9→0.43)、
  target(demeaned 0.43 / rank 0.17)、スコアのデミーン有無(0.416/0.429)、construction=magnitude。
  → **2027-08-02の1回評価は、このままでは基準値と比較できず意味を持たない**。
  評価の前に「選択窓で1.43を再現するスクリプト」を先にコミットして固定すること。
  **研究の数値を記録するときは、それを生成したスクリプトを必ず同時にコミットする。**
- ★★2026-08-02 **決着: 気配不要ML v2 は選択窓の時点で基準未達＝1.43は誤り**
  （`scripts/verify_quotefree_ml_v2.py` / `data/jp_quotefree_v2_verify/summary.json`）。
  **先に `verify_baseline.py` が 4/4 PASS**（乖離0.1〜2.0%）＝パネルと単元BTは健全。
  その上で凍結仕様を24構成掃いて **単元BTの最良 0.462**（本仕様8/側2.0xは0.429）。
  **IC は 0.0323/t=10.15 で記録(0.0275/9.16)より高い**＝差はシグナルでなく構築側。
  記録のDD−36.8%も単元(−33〜−62%)・理想ブック(−7〜−15%)のどちらにも整合しない。
  合格基準1.0に対し再現値0.43〜0.46、かつ選択窓は楽観側（v1は選択1.30→確認0.87で−33%）
  なので**フォワード期待値は0.96でなく概ね0.3**。**2027-08-02まで待つ意味は無い**。
  1.43を根拠に使ってはいけない。
- ★★2026-08-02 **理想3.50と単元0.43の落差の正体＝借株制約。構築の問題ではない**
  （`scripts/experiment_ideal_vs_unitlot_gap.py` / `data/jp_ideal_vs_unitlot/summary.json`）。
  段階分解（選択窓・8銘柄/側・1bps/side）:

  | 段 | 内容 | Sharpe |
  |---|---|---:|
  | A | 理想ブック・制約なし | **3.50** |
  | B | 理想ブック＋貸借/規制フィルタ | **−0.03** |
  | C | 単元・ショート制約OFF・資本¥2,000M | 3.64 |
  | D | 単元・ショート制約OFF・資本¥20M | **3.63** |
  | E | 単元・本番制約・資本¥20M | **0.43** |

  - **単元粒度はコストではない**: C(¥2,000M) 3.635 vs D(¥20M) 3.628 ＝ 資本100倍でも不変。
    1単元>予算は全行の**1.68%**だけ、建玉は中央値16/16本埋まる（16本未満の日は11.2%）。
    ＝「¥20Mの単元制約でαが潰れている」という説明は**誤り**。
  - **脚別分解で死因が確定**: LONG脚のみ Sh **0.17**（年率+3.5%）／
    SHORT脚のみ Sh **3.51**（年率+150%）／**SHORT脚に貸借+規制を入れると Sh −0.14**。
    無制約のショート上位8本のうち**貸借可はわずか35.4%**。
    ＝**αは100%ショート側にあり、その全部が借りられない銘柄に宿る**。
  - これは既知の壁と同一シグネチャ（売り制約解除実験「改善の80%が非貸借ショート由来」、
    フローα「αは100%ショート脚・非貸借6%がαの45%」、`xt_alert_flag`「αが規制銘柄側」）。
    **4例目**。気配不要MLも同じ理由で死ぬ。
  - 副産物: 記録の1.43は 0.43(完全な借株フィルタ)〜3.63(フィルタ無し)の**中間**に落ちる。
    **借株フィルタが部分的にしか効いていない実装だった**と考えると辻褄が合う。
  - ★`daily_model.portfolio_returns` は**いまだに借株フィルタを持たない**
    （`book_from_scores` は2026-07-30に修正済みだがこちらは未修正）。
    **この関数の出力を研究成績として引用しないこと**。docstringに警告を追加した。
- 2026-08-01 **保有構造を日次の横断面特徴量に変換する経路を実装**
  （`ownership_features.attach_ownership_features` / `OWNERSHIP_FEATURES_DAILY`）。
  有報は年1回だが、**開示の翌営業日から次の開示まで前方補完**して断面zにすることで、
  毎日全回転する気配不要MLの特徴量として使える（＝イベント建玉ではなく日次リバランス）。
  カバレッジ98.5%。自作ハーネス上の増分は 1bps/side で Sh0.429→0.472（+0.043）だが、
  **上記のとおり基準側が再現できていないので増分値は主張しない**（基準を固定してからやり直す）。
  - ★踏んだ罠2件（退行テスト済み）: ①**パネルの symbol は5桁("13010")・大株主側は4桁**で、
    揃えずにmergeすると重複ゼロになり**全特徴量が無言で0**になる（差分ゼロを「効果なし」と
    誤読しかける）。②**`Code=="00000"` は非上場の有報提出者**（ゴルフ場会員権会社等）で、
    全社が同一symbolに潰れ**前年比が企業をまたいで**計算される。
- 2026-08-01 **V3の再実行（00000修正後）: 判定はNO-GOのまま、ただし点推定が不安定**。
  件数は全て同一(eligible65/train23/OOS候補42/採用27)なのに、
  中央値 −0.47%→**+0.32%**・勝率48.1%→51.9%・利益集中56.4%→**43.9%**と動いた
  （同着エントリー日の並び順で建玉枠10の取り合いが変わるため）。
  利益集中<20%には両方とも未達でNO-GOは不変だが、**n=27では無関係な行の除去で
  中央値の符号が反転する**＝イベント枠の点推定は信用できない。
- ★2026-08-01 **J-Quantsに銘柄別FFW・指数用株式数・構成銘柄ウエイトのAPIは存在しない**
  （実キーで `/v2/indices/weights`・`/v2/indices/constituents`・`/v2/equities/free-float`・
  `/v2/equities/shares`・`/v2/corporate-actions` が全て403「endpoint does not exist」）。
  無料公式の `topixweight_j.csv` は6列でFFWを含まず、**前月末を翌月末に公表する1か月ラグ**
  （2026-08-01時点の最新は20260630基準）。銘柄別FFWの個人経路は**DataCube有料のみ**。
  TOPIX2026の `INPUT BLOCKED` は**恒久的な外部制約**で、収集の再実行では解けない。
- 2026-08-01 **TOPIX第2段階の公式定義を算出要領で確定**（`cal2_30_topix.pdf`）:
  追加基準=回転率0.2以上/累積96%以内、継続基準=0.14以上/97%以内。
  月次回転率=（**日次売買代金の中央値**×営業日数）÷**月末最終営業日**の浮動株時価総額、
  年間=基準日月以前12か月の**合計**。一方 **累積比率に使う浮動株時価総額は基準日月の日次平均**
  （分母が別物なので混同禁止）。累積比率の母集団は**回転率基準を満たし整理/特別注意銘柄でない
  銘柄群**に限定。上場1年未満は年換算。FFWは**0.05刻みで切り上げ**の離散値（0.05〜1.00）。

- ★2026-08-02 **暴落ディップバイ（CRASH_DIPBUY・ロングオンリー）→ NO-GO。死因は「全部β」**
  （`scripts/experiment_crash_dipbuy.py` / `docs/PREREGISTER_CRASH_DIPBUY.md`）。
  ロングオンリーなので**借株の壁は回避できた**が、別の壁に当たった。
  仕様: 等加重市場の日次 ≤ −3% の翌営業日**寄成**で買い、10営業日後**引成**。
  20銘柄・¥20M・レバなし・往復20bps・気配不要。スコアは断面zの単純平均5項目
  （5日下落・当日スパイク・大口空売り残・信用返済方向・自己資本比率）。
  **TP=+2.0σ_H / SL=−1.5σ_H（σ_H=ivol×√H）**、日足で同日に両方触れたら**SL約定とみなす**保守ルール。

  | 指標 | 値 |
  |---|---:|
  | エピソード数 | 30 |
  | 素のネット平均/回 | **+1.34%** |
  | 同期間の市場平均/回 | **+1.60%** |
  | **超過平均** | **−0.26%**（基準>0 に不合格） |
  | 超過中央値 / 勝率 / 利益集中 | +0.11% / 50.0% / 16.0% |

  - ★**素のリターンで判定していたら「勝ち」に見えた**。ロングオンリー低頻度戦略は
    **必ず同期間・同保有日数の市場超過で判定する**こと。
  - ★**TP/SLは有害**: バリアあり超過−0.26% vs **なし+0.03%**（中央値も+0.11%→+0.98%）。
    AGENTSの「ボラ高＝稼ぎ時」「SLは¥を食うボラ圧縮装置」がここでも成立＝**5例目**。
  - ★**米国夜間での条件付けは大きく悪化**: `dow_night(D+1) > −1%`（下げ止まり確認）で
    17件・超過**−1.86%**・勝率35%。**米国の落ち着きを待つと反発を取り逃す**。
  - VWAPエントリは寄成とほぼ同じ（−0.22% vs −0.26%）＝**執行場所の問題ではない**。
  - 年別超過は 2018 +1.2% / 2020 −0.1% / 2022 +0.3% / 2024 +3.8% / 2025 −0.8% / **2026 −6.1%**
    で不安定。感度セル(−4%/−5%トリガ)は超過プラスだが9件・7件で利益集中43%・62%＝標本不足。
    **結果を見た後にこれらを主仕様へ昇格させるのは禁止**。
  - 計測上の注意（判定に有利側の誤差）: 市場脚は entry日**終値**起点、戦略は entry日**始値**起点。
    戦略側だけ初日の寄→引を余分に得ているので、**真の超過は−0.26%より悪い**。NO-GOは頑健。
  - **KOSPI・中国は使っていない**。東京の寄付き時点で当日値が存在しない
    （KOSPIは09:00 KST＝同時刻、中国は10:30 JST開場）ため**当日値はルックアヘッド**。
    加えてStooqが現在このIPをブロック中で履歴自体が未取得。前日値での再挑戦は別事前登録で。

- ★2026-08-02 **暴落後の指数タイミング（1306現物・銘柄選択なし）→ NO-GO。バイ&ホールドに完敗**
  （`scripts/experiment_crash_index_timing.py` / `docs/PREREGISTER_CRASH_INDEX_TIMING.md`）。
  ディップバイの分解で「反発は市場に宿り銘柄選択に情報が無い」と判明したので、
  選択を捨てて指数エクスポージャのタイミングだけを取る形を検証した。

  | | Sharpe | 年率 | DD | 投資日数比率 |
  |---|---:|---:|---:|---:|
  | 暴落後タイミング(−3%/10日保有) | **0.174** | +1.8% | −23.5% | 8.9% |
  | **1306 バイ&ホールド** | **0.583** | +11.0% | −35.2% | 100% |

  感度も全滅（−4% Sh0.295 / −5% Sh0.451 / 保有5日 Sh0.008 / いずれもB&H 0.583未満）。
  **DDだけ浅いのは市場に居ないからで、リスク調整後は明確に劣後**。
  ＝暴落後の反発は「1日あたり」では魅力的に見えても、**その期間のボラが高すぎて
  リスク調整すると残らない**。年間の他の期間の上昇を捨てる代償の方が大きい。
  **タイミングで市場に勝つ方向はこれで閉じる**（先物版はR8で既に全滅・税区分でも対象外）。

- ★★★2026-08-02 **`ret`（終値→終値）に隣接セッション判定が無かった＝schema v7 の取りこぼし**。
  `ret_on_fwd`/`ret_cc_fwd` には2026-07-30に判定を入れたが、**`ret` は素の
  `groupby.pct_change` のまま**だった。パネルは流動性フィルタ済みで行が連続しないので、
  一度ユニバースを外れて再参入した銘柄の**数週間ぶんの値動きを1日のリターンとして拾う**。
  銘柄は**上昇・出来高急増のあとに再参入する**のでバイアスは系統的にプラス。実測:

  | | 値 |
  |---|---:|
  | 前行が非隣接セッションの行 | **106,228行（全体の10.71%）** |
  | その行の平均リターン | **+128.3bps**（隣接行 +2.7bps＝**47倍**）・std 14.38% |
  | 等加重市場の年率 | **+39.9% → +6.5%**（修正後） |
  | 整合性 `intraday_ret+overnight_gap` | +6.3% ← 修正後と一致（修正前は不整合） |

  修正済み（`_bwd_ok` を追加・schema v11・退行テスト `BackwardReturnGuardTest`）。
  **`ret` を市場リターンやベンチマークに使っていた分析は全て再実行が必要**。
- ★★★2026-08-02 **`ensemble_core` の記録Sharpeは、この汚染に約26%依存している**。
  `cc` は `ret` だけでなく **`ivol` と `amihud20`（どちらも本番 BASE_FEATURES）**にも流れる。
  切り分け実測（`verify_baseline.py`）:

  | 構成 | 単元OOS24+ Sharpe | 判定 |
  |---|---:|---|
  | `ret`のみ修正・`ivol`/`amihud20`は据え置き | **3.433** | 4/4 PASS（記録を完全再現） |
  | `ivol`/`amihud20` も同じ汚染を除去 | **2.623** | 2/4 FAIL（全期間は1.775→1.283） |

  ＝**汚染された `ivol` は「最近ユニバースを出入りした＝流動性レジームが変わった」ことの
  ノイズ混じりの符号化として実際に予測力を持っている**。ボラ推定としては誤りだが、
  除去すると成績が26%落ちる。**どちらを正とするかは本番構成の判断なのでユーザー預かり**とし、
  現状は安全側（`ret`のみ修正＝4/4 PASS）で止めてある。
  判断する場合の論点: 正しいボラ推定を採って基準値を2.623へ引き下げるか、
  現行の符号化を維持して「ivolは純粋なボラでなく流動性レジーム混じり」と明記するか。
- 2026-08-02 **リバーサルを脚別×借株可否で24セル掃引 → 全滅**
  （`scripts/experiment_reversal_leg_sweep.py`・選択窓2021-2024・形成1/5/20/60日×
  raw/業種中立×保有1/5/20日・気配不要・上下10%分位・1bps/side）。

  | 列 | 最良 | 意味 |
  |---|---:|---|
  | `long_excess`（負け組ロング−市場） | **+0.185** | 唯一執行可能な列。ほぼ全セルが0近傍〜負 |
  | `short_free`（勝ち組ショート・無制約） | **+0.496** | 短期保有で実在 |
  | `short_borrow`（貸借+規制で絞る） | **−0.051** | **全セル負**＝6例目の借株の壁 |
  | `ls_borrow` | +0.162 | 実取引可能な合成は無価値 |

  ＝**リバーサルαも短期のショート側にあり、借りられる銘柄に限ると消える**。
  ロング側（負け組を買う）は市場超過がほぼゼロ。条件（形成長・業種中立・保有日数）を
  変えても構造は同じ。**基準1.0に対し最良0.185で、条件変更では届かない**。
  ※初回実行では `long_excess` が −2.2〜−3.6 と出たが、**それは上記 `ret` バグによる
  ベンチマーク水増しが原因**で、修正後は0近傍。バグ発見の端緒になった。

- ★★2026-08-03 **気配候補プリスクリーン（QUOTE_SHORTLIST）→ 事前登録基準は NO-GO。
  ただし「ユニバースを削るとαが死ぬ」は流動性フロアで削った場合の話で、
  シグナルで絞ると水準は残る**（`scripts/experiment_quote_shortlist.py`）。
  前日確定の `gap_vol60`（残差ギャップの60日ボラ・PIT）上位K銘柄だけに
  `ensemble_core` の最終選択を限定したときの単元OOS24+（本番制約・7bps/side）:

  | K | kabu取得目安 | Sharpe | 維持率 |
  |---:|---:|---:|---:|
  | 30 | ~3秒 | 1.553 | 45.2% |
  | **50** | **~5秒** | **1.685** | **49.1%** |
  | 100 | ~11秒 | 1.366 | 39.8% |
  | 200 | ~21秒 | 1.718 | 50.0% |
  | 全467 | 49-66秒 | **3.433** | 100% |

  **判定は NO-GO**（事前登録「維持率≥50%」に 49.1% で0.9pt未達。絶対Sharpe≥1.0 は通過）。
  - ★**K=50 の 1.685 は台帳で最良の執行可能シミュレーション値**（従来最良は先物ナイト×β 0.77）。
    ただし**Kカーブが非単調**（K=100 で 1.366 と凹む）＝**推定誤差が大きく、
    1.685 を精密な値として扱ってはいけない**。
  - **含意**: kabu のスループット（~9.5銘柄/秒）でも **50銘柄なら約5秒**で取れる。
    これは「他ブローカーに必要」としていた 95銘柄/秒 相当の同時性を kabu のまま得る形。
    AGENTS の「**気配が寄値を予測しないという実測は66秒スメアと分離できていない**」が
    効いてくる: スメアが1/12になるので、**ユーザーの実弾NO-GO判定の前提は変わりうる**。
  - ★**ただしこれは同時性の話だけで、気配そのものの予測力は未実測**。
    「気配問題が解決した」と読んではいけない。次にやるなら
    ①50銘柄の気配スナップショット実測 → ②`analyze_quotesnap.py --book` で建玉一致率 →
    ③既存対応表（≥75%GO / 50-75%減額 / <50%NO-GO）→ ④ユーザー承認、の順。
  - αが半分になる事実は変わらない（維持率~49%）。ユニバース縮小は**タダではない**。

- ★★2026-08-03 **日経225OP カレンダースプレッド → 事前登録5基準を全通過（この枠で初）。
  ただし PAPER ONLY で、生死はスプレッド実測にかかる**
  （`scripts/experiment_option_calendar.py` / `docs/PREREGISTER_OPTION_CALENDAR.md`）。
  同一ATMストライクで**期近を売り・期先を買う**ネットデビット構成。週1回・保有5営業日。
  シグナルは IV の水準でも同一限月スキューでもなく、**期間構造の傾き**
  `IV_near − IV_far` の252日z ≥ +0.5。既存2本（IV−RV / OTMスキュー）と別の情報源。

  | 指標 | 値 | 基準 |
  |---|---:|---|
  | 取引数 | 86 | ≥30 ✅ |
  | Sharpe | **1.97** | ≥1.0 ✅ |
  | 勝率 / 平均 / 中央値 | 68.6% / +7.20% / +9.37% | — |
  | 最大利益取引の利益シェア | 5.3% | <20% ✅ |
  | 最悪取引 | −71.4%（**デビット内**） | >−100% ✅ |
  | 負の年 | **0**（2019-2026全年プラス） | ≤3 ✅ |

  - **裸売りの死因を機構的に排除できている**（ストラドルは1回で−144%、これは最悪−71%で
    デビットに収まる）＝定義済みリスクが実際に効いていることを実測で確認。
  - ★**検証済み: `Settle` は理論値ではない**。全1,389,620行のうち `Settle==Theo` は2.7%、
    出来高>0の行でも7.5%。「損益がモデルの巻き戻し」という疑いは晴れた。
  - ★★**急所はコスト**。事前登録の4ティック控除で1.97だが:

    | 控除 | 4 | 10 | **20** | 30 | 50 |
    |---|---:|---:|---:|---:|---:|
    | Sharpe | 1.97 | 1.54 | **0.83** | 0.12 | −1.29 |

    **損益分岐は約17-18ティック**。2レッグ×往復＝4回のスプレッド越えなので、
    現実的なレンジと正面から重なる。**bid/ask が無い以上ここは決められない**＝PAPER ONLY。
  - ★★**2026-08-03 訂正: put/call のラベルが逆だった**。`PCDiv=1` は**プット**
    （低ストライクが安く高ストライクが高い形。パリティ C−P≈S−K でも検算済み）。
    **通ったのは一貫してプットのカレンダー**で、数値(1.97)は不変・ラベルのみ訂正。
  - ★★**「put版は0.17」も誤り＝選択の副作用だった**。call側は独自のATM/流動性フィルタを
    通すので**建玉が別物**になっていた。**同一の(日・ストライク・限月)で組み直すと
    call は Sharpe 1.01 / 平均+4.04%**（put 1.97 / +7.20%）。
    「片側だけ効く」という非対称は**存在しない**。
  - ★★**ただしパリティ違反が残る**: カレンダーは同一Kなら put版 − call版 =
    `K×(割引率の差)` の**決定論的定数**のはずなのに、実測は相関+0.647・
    **差の標準偏差がデビットの23.4%（約90ポイント）**。
    ＝**清算値がパリティ整合でない**。したがって **1.97 を精密値として扱ってはいけない**。
    同じ経済実体で 1.0〜2.0 の幅が出る。基準1.0は下限側でも辛うじて満たすが余裕はない。
  - 規模: デビット中央値 ¥395（×1000倍 = 1組¥395,000）。1組あたり平均損益 ¥35,256。
  - ★★★**2026-08-04 敵対的検証で棄却（同一バー・アーティファクト）**
    （`scripts/verify_option_calendar_execution.py` / `execution_verification.json`）。
    基準の1.97は**シグナル（D日清算値のIV）と建玉（D日清算値）が同じ数字**で成立していた。
    清算値は引け後に確定するので「その日のzを見てその日の清算値で建てる」は実行不能。
    さらに両者は清算値ノイズで機械的に結合している: 期近の清算値がεだけ高く出ると
    **傾きが膨らんでシグナルが発火し、同時にデビットが安くなって建値が有利になる**＝
    同日建てはノイズの平均回帰を純粋に収穫する。パリティ違反（デビット比23%のσ）が
    そのノイズの大きさの直接証拠。

    | 形 | n | Sharpe | 平均 | 最悪 |
    |---|---:|---:|---:|---:|
    | 同日建て（実行不能な基準） | 86 | 1.97 | +7.20% | −71% |
    | **T+1建て（実行可能形）** | 82 | **0.47** | +2.05% | **−89%** |
    | パリティ平均・T+1 | 82 | **0.96** | +4.14% | −79% |
    | T+1の前半/後半 | 34/48 | **−0.45 / +1.29** | | 不安定 |

    実行可能形は基準1.0未達（0.47〜0.96）かつ前後半で符号が割れる。**NO-GO**。
    ナイトセッション（17:00〜）で同日夕方に建てれば真値は0.47と1.97の間だが、
    その検証にはナイトの実板が要る（清算値では測れない）。実板スプレッド測定より
    **先にこの欠陥で死んでいた**＝スプレッドの実測はもはや不要。
    ★教訓: **シグナルと執行価格が同じスナップショットから来る backtest は、
    その2つがノイズで結合していないかを必ず先に検証する**（quotesnapのσ_ε分解と同型）。
- 2026-08-03 **他系統の棚卸し（暗号・米日リードラグ・スクリーナ）**:
  - `trading/bybit`（単一銘柄TA）… 最良 ETHUSDT Sharpe 0.88 / DOGE 0.77、多くは負、cfl版は全て負
  - `backtest`（米日セクターリードラグPCA）… R/R 0.85（全期間）だがCV testは平均~0.6で不安定
  - `screener`（バリュー）… 既知の確認窓 Sh0.72
  - ★**暗号のクロスセクションは検証不能**と判定。Bybit公開APIは USDT perp 712銘柄・
    日足2020-03〜・funding も取れるが、**instruments-info は Trading のみで上場廃止銘柄が0件**。
    2021-01以前に上場して今も生きているのは**わずか5銘柄**（BCH/BTC/LINK/LTC/XTZ）で、
    **サバイバーシップバイアスの上限すら見積もれない**。アルトのクロスセクションはやらない。
    （majors限定の時系列＝ファンディングキャリー等は検証可能。ただし暗号は雑所得で最大55%）

- 2026-08-03 **オプションの指値・スプレッド発注の可否（一次情報）**:
  - **大阪取引所のストラテジー取引（複数限月を同時約定させる制度）は
    カレンダースプレッド（国債/金利/商品先物）と（指数先物）の2種類のみで、
    オプションは対象外**（[ストラテジー取引制度要綱](https://www.jpx.co.jp/derivatives/rules/outline/tvdivq0000003mke-att/nlsgeu000000v92g.pdf)
    2026-04-13現在）。＝**オプションのカレンダーは1銘柄として建てられない。必ず2本のレッグを別注文で組む。**
  - 指値自体は可能（`FrontOrderType 20=指値`）。kabu に `/sendorder/option` が
    別途あるという情報があるが**公式仕様で未確認**（AGENTS既存記述は `/sendorder/future` のみ）。実装前に要確認。
  - ★**レッグ組みの固有リスク**: 期近の売りだけ約定して期先の買いが未約定だと**裸のコール/プット売り**が残る
    ＝ストラドルを1回で−144%にした死因そのもの。**必ず期先の買いから約定させる**こと
    （片肺でも「買っただけ」＝損失は払った額に限定される）。逆順は禁止。
  - リポジトリの既存2例（スキャルピングのメイカー転換=ハーフスプレッドの17.5-22%しか残らない、
    ギャップ反転のパッシブ指値=グロスが−0.19→−6.37bpsに悪化）は**いずれも日中・分単位で
    αが減衰する文脈**。本カレンダーは週1シグナル・5営業日保有なので**1日かけて指値を置ける**＝
    逆選択の影響は桁違いに小さいはず。**過去2例をそのまま当てはめる理由はない。**
  - → 測るべきは「スプレッドは何ティックか」ではなく
    **①ATM近辺の期近・期先で1日以内にメイカー約定できる確率 ②約定後の逆選択 ③期先→期近順での片肺率**。

- ★★2026-08-04 **決算発表前ドリフト（EARNINGS_TIMING）→ 選択窓・確認窓の両方を通過。
  ただし水準は限界的で、Sharpeが「規約」で1.0を跨ぐ**
  （`scripts/experiment_earnings_timing.py` / `docs/PREREGISTER_EARNINGS_TIMING.md`）。
  ★**構造が今までと違う**: αを**ロング側**（予想発表日の1〜5営業日前の銘柄を等加重買い）に置き、
  ショートは指数ヘッジ。**ショート銘柄がαを担わない**ので、直近7例を殺した借株の壁を踏まない。

  | | 選択窓2022-24 | 確認窓2025-26 |
  |---|---:|---:|
  | 稼働日のみSharpe（事前登録の実装） | **1.10** | **1.90** |
  | **全暦日Sharpe（休止日を0とする）** | **0.71** | **1.17** |
  | 年率（全暦日） | +4.2% | +6.6% |
  | 最大DD | −5.0% | −4.7% |
  | 稼働率 | 41.7% | 38.4% |

  - ★**事前登録が年率化の規約を書いていなかった**。稼働日のみなら両窓通過、
    全暦日なら選択窓0.71で未達。**都合の良い方を採らない**。単独運用なら全暦日が正しい。
  - **ヘッジは執行可能な形に置換した方が良い**: 等加重市場ショート（約400銘柄＝実行不能）より
    **TOPIX(1306)ショートの方が成績も良い**（稼働日Sharpe 選択1.26/確認2.11）。
  - **DDが小さい**（−4.4〜−5.9%）のは他戦略に無い特徴。年率は+6〜8%と控えめ。
  - ★**副仕様B（発表遅延ショート）は Sharpe −3.71 で全年マイナス**＝
    「遅れる決算は悪い知らせ」という定型事実は**日本株では逆**（遅延銘柄はむしろ上がる）。棄却。
  - ★**`/v2/equities/earnings-calendar` の正しい意味は「翌営業日の公式発表予定」**
    （v1 `/fins/announcement` の後継。2026-08-08 再検証: 土曜に叩くと翌月曜分186件が返る。
    初回調査の「当日分」という解釈は誤りだった）。date/from/to/code は全て無視・
    pagination なし・**履歴なし**＝バックテスト不可は変わらず。ダッシュボードが毎朝
    append-only 保存して履歴を自作している（`data/jp_earnings_calendar/`）。
    過去の予想発表日は `fins` の前年同期 `DiscDate` +364日で自前構成
    （実績との差は**中央値1日・92.4%が±5日以内**）。
  - ★★**2026-08-04 追検証で棄却。利益集中がこのリポジトリの基準を大きく外れる**:

    | 除外 | 選択窓 稼働Sh | 確認窓 稼働Sh |
    |---|---:|---:|
    | なし | 1.26 | 2.10 |
    | 上位3日を除く | 0.56 | 1.18 |
    | 上位5日を除く | **0.12** | 0.63 |
    | 上位10日を除く | **−0.68** | **−0.82** |

    **上位5日で総利益の92.2%（選択窓）・75.2%（確認窓）**、上位10日で140%/128%
    ＝残りの日は合計マイナス。**306日中10日(3.3%)を抜くと死ぬ**。
    過去にこの基準で落とした増配×低PBR(64.7%)・固定株縮減(43.9%)より悪い。
  - ★**集中しているのは「日」であって「銘柄」ではない**。銘柄日の超過リターン分布は
    p5−2.44/中央−0.02/p95+2.59%・歪度+0.44と健全で、上位1%の銘柄日は総利益の13%程度。
    ＝**特定の数日にバスケット全体がまとめて動いている**。本物の発表前ドリフトなら
    利益は多数の銘柄・日に薄く広く積み上がるはず。βはヘッジ済み(実現β+0.002)なので
    単純な市場βではないが、**残った共通要因（セクター/サイズ等）を拾っている疑い**が濃い。
  - ★**事前登録に利益集中の基準を入れ忘れていた**（Sharpeと負の年数だけだった）。
    他ファミリーには課してきた基準なので不備。**今後の事前登録には必ず含める**。
  - β中立化の検証（ユーザー質問）: バスケットのPIT βは0.81〜0.95で1に近く、
    **1:1金額中立が既にほぼβ中立**（選択窓の実現β +0.002）。PIT βでヘッジすると
    ヘッジ量が減るぶん**実現βはむしろ+0.13に増え**、確認窓での見かけの改善
    (2.10→3.11)は上昇相場での残存βによるもの。**β中立化は不要**＝AGENTS既存の
    「βはリスク管理としてもαとしても導入不要」が2例目として再現された。

- ★★2026-08-04 **FXは8仮説＋バイ&ホールドが全滅＝日次〜週次のG8為替にαは見つからない**
  （`scripts/experiment_fx_basket.py` / `experiment_fx_carry_dip.py` / `experiment_fx_alpha_sweep.py`）。
  Dukascopy実測bid/ask・金利差スワップ（曜日規約つき）・全暦日Sharpe・利益集中基準で統一検証:

  | 仮説 | 選択窓 Sh | 備考 |
  |---|---:|---|
  | クロスセクション・キャリー | 0.138 | 確認窓0.069。スワップ年+2.3%を価格変動が食う |
  | 12週モメンタム | 0.549 | 確認窓0.233・上位10週除去で−0.52に反転 |
  | 正キャリーのディップバイ | −0.214 | 「暴落後は安い」が不成立（勝率50.9%・平均−0.11%） |
  | バイ&ホールド最良（USDJPYロング） | 0.53 | 円安15年の単一経路。正キャリー全ペア等加重は**−0.09** |
  | ドル・タイミング（LRV） | −0.06 | 確認窓は未開封のまま温存 |
  | 長期リバーサル（名目5年・ECB履歴） | 0.114 | 同上。※JPYのCPIが2021-06で死んでおりPPP実質版は構成不能 |
  | 金利モメンタム（Δ3か月・1か月ラグ） | −0.075 | 同上 |
  | 週次リバーサル | −0.364 | 同上・9年中7年マイナス |

  - **構造は一貫**: 金利差は年+1〜2%しか無いのに為替ボラは年8〜11%。方向を当てられない限り
    Sharpe 1に届かない。UIP（カバーなし金利平価）が概ね成立していた。
  - 多重性: 後半4本は同時試行。選択窓で全滅したため確認窓(2020+)は**4本とも未消費**。
  - ★2026-08-05 **日中1本目=ゴトー日・東京仲値（USDJPY）→ 時間足の定義では NO-GO。
    ただし検証過程でバグ2件と訂正1件**（`scripts/experiment_fx_gotobi.py`）:
    - ★バグ: 営業日カレンダーを株式パネル（**2018年開始**）から取っていたため、
      **2011-2017の7年分のゴトー日が全て非ゴトー側へ誤分類**されていた（ゴトー日605→1,102日）。
      J-Quants `/v2/markets/calendar`（2010-2027・HolDiv）へ差し替えて修正。
      **営業日カレンダーが要る検証は必ずこのカレンダーを使うこと**
      （`data/fx_rates/jp_market_calendar.parquet`）。
    - 訂正の顛末: 初回「+1.45bps優位」→ スプレッド差の混入と判明し一旦撤回 →
      **カレンダー修正後は mid→mid でも +0.85bps/日の優位が復活**（対照差 t=3.36）。
      ＝**ゴトー日の朝のドル高ドリフトは実在する**（Ito–Yamada と整合）。
      副次: Dukascopyのスプレッドは**ゴトー日朝だけ約1.4bps細い**（流動性の季節性も実在）。
    - **それでも取れない**: GMO FXネオの0.2銭原則固定（ユーザーの執行口座・9-翌3時で
      取引窓を包含）を当てても **+0.63bps/トレード・年間Sharpe 0.28**（基準1.0）。
      勝率54%・負の年多数。時間足の 09:00→10:00 定義では NO-GO。
    - ★**時間足では原理的に決着しない残り**: 古典形は「9:55の仲値に向けて上げ→仲値後に反落」で、
      09:00→10:00バーは**上げと反落を相殺**して見せる。買い09:00→売り09:55 の検証には
      分足以下が必要 → **該当日の00時UTC台ティックだけをピンポイント収集中**
      （ゴトー1,102日+対照・約2,200ファイル＝フル分足収集の1/30。
      `scripts/collect_dukascopy_fix_ticks.py`・再開可能・スロットル下で数日）。
      収集完了後、`PREREGISTER_FX_GOTOBI.md` の枠内（感度分析）として1回だけ評価する。
  - ★2026-08-06 **ゴトー日ティック形（9:00→9:55）→ Sharpe 0.856 で NO-GO。
    これまでで最も基準に近い**（`data/fx_ticks_fix/` 1,080万ティック・2,199日）。
    **反落相殺仮説は実証された**: mid→midグロスは時間足の+0.83bpsに対し**+2.13bps**
    （2.6倍＝仲値後の反落がバーの中で食っていた）。GMO 0.2銭ネット**+1.93bps/回**・
    対照差t=+2.02・負の年2/9・集中12.1%・exTop10 0.57と**Sharpe以外の全基準を通過**。
    しかし全暦日Sharpe 0.856 < 1.0。事前登録どおり確認窓2020+は**未開封のまま温存**。
    → ★2026-08-06 **封印再判定を事前登録**（`docs/PREREGISTER_FX_GOTOBI_FORWARD.md`・
    ユーザー承認済みのセカンドチャンス。R2-Bの「新しい未使用期間で判定式を事前固定して
    1回だけ」規則に準拠）。評価セット＝**封印済み2020-2026（未閲覧）＋フォワード2026-08以降**
    （合計約7.5年・約560取引・Sharpe標準誤差≈0.37）。**判定日2027-08-06・1回のみ**。
    `verify_gotobi_final.py` に日付ガード実装（前倒し実行を機械的に拒否）。
    フォワードは `collect_gotobi_forward.py`（**生ティックのみ保存・成績は一切計算しない**
    no-peek設計・キャッチアップ型）＋launchd毎日12:00（`jp.finance.gotobi-forward`）。
    ※Dukascopyは過去ファイルを恒久配信しているため、スケジューラが止まっても
    判定日に1回キャッチアップすれば全量回収できる。**落ちたら恒久クローズ（三度目なし）**。
    年別bps: 2011:+286/2012:−28/2013:+170/2014:−22/2015:+70/2016:+146/2017:+381/2018:+129/2019:+96。
  - 2026-08-06 **時間帯セズナリティWF（高頻度主軸・72セル・凍結規則で毎年再選択）→ 完敗**
    （`scripts/experiment_fx_hourly_seasonality.py`）。全リターンOOSのウォークフォワードで
    **Sharpe 0.11・後半2020+は−0.21・負の年6/13**。選択セルが年ごとに入れ替わり
    （EURUSD17時→USDJPY2時→EURUSD11時）、**t≥2.5の時間帯効果はOOSで持続しない**。
    ＝FXの時間帯偏りは選択窓のノイズ。JP株の「選択窓+0.28→確認窓−0.14」と同型。
  - **残る日中フロンティア**: 指標発表イベント（要イベント時刻データ）、セッション間フロー、
    仲値の分足精査（分足はスロットル解消後 or Windows VPS or OANDAトークンで取得可能）。
  - 資産は残る: 7ペア15年の実測bid/ask日足・8通貨金利・曜日規約つきスワップモデル
    （`trading/fx/swap.py`・テスト8本）・ECB仲値27年。FX（店頭）は申告分離20.315%で
    先物・オプションと同一区分（株式とは通算不可）。

- 2026-08-06 **診断: 長期保有スクリーニングにおけるセクターモメンタムの寄与**
  （`scripts/analyze_sector_momentum.py` / `data/jp_sector_momentum/summary.json`・
  業種指数33本を2008年まで遡って新規収集 `sector_indices_2008_2026.parquet`）。
  ユーザーの体感「長期スクリーンはセクターモメンタム要素が大きい」を定量化した。**診断であり戦略ではない**:
  - **A. 断面分散のセクター寄与はわずか5-6%**（1m 4.8% / 3m 5.1% / 6m 5.3% / 12m 6.2%。
    2018-2026・サバイバーシップ安全パネル）。長期でも銘柄リターンの9割超は業種外の要因。
  - **B. ただし12-1モメンタムスクリーンとセクターモメンタムの順位相関は ρ≈0.25**
    ＝**上位銘柄が同じ業種に固まって見える体感は実在する**。しかし上位10%銘柄の
    モメンタム水準のうちセクター平均由来は**8.2%だけ**＝固まりは同時相関であって主因ではない。
  - **C. セクターモメンタム自体に持続性が無い**（業種指数18年・F∈{3,6,12}m×H∈{1,3,6,12}mの
    全12セルで |rank IC| ≤ 0.036・最大t=1.28（F12/H1）・上位-下位スプレッド最大でも
    12か月+2.43%で非有意）。日本の銘柄モメンタム不在（既知）は**セクター粒度でも同じ**。
  - **含意**: 長期スクリーンで「勢いのある業種」に乗るのは**期待リターンの裏づけなく
    集中リスクだけ足す**行為。スクリーンを業種内順位（sector demean）にしても
    シグナル水準の9割以上が保存される。※価格指数ベース（配当抜き）・コスト無しの診断。
    戦略化するなら別途事前登録が必要。
- 2026-08-06 **診断続編: 相関ベースの統計クラスタ vs S33**（ユーザー提案
  「連動が極めて強い銘柄グルーピング」の検証。`scripts/analyze_comovement_clusters.py`・
  前年252日相関でWard階層クラスタK=33をPIT形成→翌年で評価＝全数字OOS）:
  - ★**手法の罠**: average linkage は**退化**する（1クラスタに88%が吸われ中央値サイズ1）。
    Ward で均衡分割（最大9%・中央値12銘柄）。**相関クラスタは必ずサイズ分布を検査すること**。
  - **A. OOS分散シェアはクラスタが勝つが小差**: 1m **20.2% vs S33 15.6%**・3m 18.1% vs 13.8%
    （相対+30%）。※全銘柄では5-6%だったのが流動ユニバース(¥5億+)では15-20%＝
    **共動構造は大型株の現象**という副次発見。
  - **B. グループ内平均ペア相関はOOSで同じ**（0.411 vs 0.411）＝S33の濃い業種（銀行等）は
    既に相関クラスタであり、Wardの改善は雑多業種の分割と業種横断テーマの統合から来る。
  - **C. クラスタは年次で不安定**: 同居ペアの翌年生存率**29.5%**（偶然基準3%よりは遥かに
    高いが、毎年約7割の同居関係が解消）。使うなら毎年再形成が必須。
  - **D. クラスタ・モメンタムにも持続性なし**: 12-1形成→翌1m IC 0.045(t=1.23)/3m 0.017(t=0.29)。
    **「真の連動グループの勢いに乗る」も不成立**＝グルーピングを賢くしてもモメンタム不在は
    変わらない（業種・クラスタ両粒度で確認）。
  - **含意**: 統計クラスタの正しい用途は**リスク管理**（クラスタ内デミーン・クラスタ別
    エクスポージャ上限）であって**リターン源ではない**。R2のクラスタ残差棄却（日中）とも整合。

- 2026-08-06 **診断: 通貨強弱の状態空間モデル（@Chisiki1のフレームワーク）を実データで検証**。
  モデル＝潜在強弱s＋モメンタムAR(1)（m'=ρm, 0≤ρ<1）＋ネットワーク公正価格からの
  乖離AR(1)（d'=φd, |φ|<1）＋「|μ|>λσ+コスト のときだけ売買」のゲート。
  クロスペア EURJPY/GBPJPY/EURGBP の日足bid/askを新規収集して2エンジンを測った:
  - **エンジン1（モメンタム）: 燃料が無い**。Δ強弱のAR(1)は日次−0.008・週次−0.062・
    月次−0.072（7通貨・2011-2025）＝モデルの仮定 0≤ρ<1 に対し実測はゼロ〜負。
    既存3検証（12週モメンタム崩壊・週次リバーサル負・セズナリティWF死）と整合。
  - **エンジン2（乖離）: 実在するが市場メーカーの領分**。EURJPY vs EURUSD×USDJPY の
    乖離dは中央値0.16bps・|d|>1bpsが18.8%の日・φ=+0.43（半減期0.8日）＝AR(1)仮定は正しい。
    T+1建て（同一バー結合を排除）でも |d|>1bps の日に**グロス+1.36bps/回**が残る。
    GMO 3レッグ0.72bpsならネット+0.64bpsに見えるが、**急所は venue 整合性**:
    dはDukascopyのクォートで測っており、GMOで建てる場合「GMOのdがDukaのdと一致する」
    という**検証不能な前提**が要る（ECBは構成上d≡0で照合に使えない）。
    同一venue（Duka実測スプレッド1.95bps）では **1.36 < 1.95 で死ぬ**。
  - **総評**: 枠組み自体は正しい形式化（我々の強弱分解と同一・ゲートは我々の規律と同型）。
    ただしG8日次では**ゲートがほぼ常にNO TRADEを出す**のが実測＝13仮説全滅と同じ結論を
    モデルの言葉で言い直したものになる。作者の「資金効率はそこまでよくない」は正直で、
    実測はさらに厳しい。
- 2026-08-06 **Web調査由来のカレンダー・アノマリー3本 → 選択窓で全滅**
  （`scripts/experiment_jp_calendar_sweep.py` / `docs/PREREGISTER_JP_CALENDAR_SWEEP.md`・
  TOPIX指数2008-2026を新規取得・先物往復1bp・文献方向で凍結・選択2010-2019）:

  | セル | 文献 | 選択窓Sh | 備考 |
  |---|---|---:|---|
  | TOM（月末→翌月3営業日ロング） | Quantpediaで指数横断の最頑健 | **−0.238** | 10年中7年マイナス＝現代のTOPIXでは**逆** |
  | 休日前ロング | Ziembaの日本の休日効果 | +0.028 | 消滅 |
  | 月曜ショート | 週末効果の古典 | −0.213 | 消滅 |
  | SQ週（診断のみ） | — | +0.231 | 特記なし |

  確認窓2020+は3本とも未開封のまま温存。**公知のカレンダー効果は現代日本では死んでいる**
  （元文献の多くは1949-1988年データ。公開されたエッジほど裁定済み、の直接例）。
  未検討で残した候補と理由: 優待先回り（優待データ未保有）・月別効果/ETF分配金売り/
  配当落ちリバウンド（年1-2回＝ユーザー方針で低頻度は優先度落とし）・指数入替（TOPIX2026で別枠）。
- 2026-08-08 **バリューイベントV4（増配×低PBR・母集団の流動性再定義）→ NO-GO。
  ただし基準1つ・6pt差＝イベント系で最も接近**（`scripts/run_value_event_v4.py` /
  `docs/PREREGISTER_VALUE_EVENT_V4_LIQUIDITY.md`）。
  V1-V3の確定知見「問題は標本供給。流動性制約と両立する母集団の再定義が必要」への回答として、
  **唯一の変更=フロア¥10億→¥1億**（60営業日保有の参加率検算: 1案件¥200万・3日分割で0.7%）。
  シグナル・モデル・保有はV1凍結仕様のまま。コストは40bpsに引き上げ（小型株保守）。

  | 指標 | V1(¥10億) | **V4(¥1億)** | 基準 |
  |---|---:|---:|---|
  | OOS採用 | 17件 | **61件** | ≥30 ✅ |
  | 中央値(コスト後) | +4.84% | **+4.61%** | >0 ✅ |
  | 勝率 | 64.7% | 55.7% | — |
  | **利益集中** | 64.7% | **25.96%** | **<20% ❌** |
  | 増分のみ(V1未評価50件)の中央値 | — | **+4.12%** | >0 ✅ |

  - **中央値+4.6%は標本を5倍にしても頑健**＝増配×低PBRのイベント後ドリフトは実在する公算。
    集中も64.7%→26.0%へ大幅改善したが、上位1件（+57%/60日）が利益の26%を占め基準に届かず。
  - 増分母集団（消費済み窓に依存しない50件）単独でも中央値+4.12%＝V1の23件への依存はない。
  - **事前登録どおりV5（三度目の母集団変更）はしない**。残る道は「同一凍結仕様の
    フォワード観測」（2026-05以降の新規イベントで再判定。要finsの継続収集）のみ。
  - ES(5%)−23.0%・最大損失−24.3%＝小型バリューの60日保有は裾が重いことも記録。

- 2026-08-08 **米指数CFD 4セル（初の米国検証）→ 全滅**
  （`scripts/experiment_us_index_sweep.py` / `docs/PREREGISTER_US_INDEX_SWEEP.md`・
  OANDA実測bid/ask・NY時間DST処理・CFD金利=(USD3M+2.5%)/365モデル）:

  | セル | 文献 | SPX選択窓 | 死因 |
  |---|---|---:|---|
  | H1 日中モメンタム(寄30分→引30分) | Gao-Han-Li-Zhou | **Sh−1.33** | 発表(2018・標本93-13年)後に**符号反転**＝公開後の裁定 |
  | H2 米TOM | Quantpedia最頑健 | 0.00 | スプレッド+金利で消滅 |
  | H3 夜間ドリフト | 株式プレミアムは夜間 | −0.28 | **CFD金利1.9bps/泊が夜間3bpsを食う**＝ビークル不適合 |
  | H4 ATH−15%押し目・長期保有 | — | 0.22 vs **B&H 0.45** | JP同様、**タイミングはB&Hに勝てない**（NAS も 0.24 vs 0.60） |

  - 確認窓2020+は4セルとも未開封のまま温存（選択窓全滅のため）。NAS100も同結果。
  - **教訓の再確認**: ①有名アノマリーは公開後に死ぬ（H1は符号反転まで）
    ②夜間系はCFDの金利構造と根本的に相性が悪い（現物ETFか先物が必要）
    ③指数タイミングでB&Hに勝つ試みはJP/US両方で棄却＝**この方向は閉じる**。
  - 長期エントリータイミングで生きているのは**銘柄レベルのイベント駆動のみ**
    （V4増配×低PBR: 中央値+4.6%・集中6pt差でNO-GO・フォワード継続が唯一の道）。

- 2026-08-08 **V4増配×低PBRのフォワード封印体制を構築（SEALED→2027-12-01）**。
  V4は集中1点のみでNO-GOだったため、仕様不変のまま新期間2026-05-01〜2027-08-31の
  イベント供給で1回だけ再判定する（`docs/PREREGISTER_VALUE_EVENT_V4_FORWARD.md`）。
  - fins静的スナップショットは2026-04-24で停止していた → `collect_fins_incremental.py`
    が日次増分収集（`fins_incremental.json.gz`・load_finsが自動統合・途中保存つき）
  - `collect_v4_forward_events.py` が凍結仕様の適格イベントを追記専用台帳
    `data/value_event_v4_forward/events.jsonl` に記録（**リターンは一切計算しない**。
    凍結Ridge=学習99件の予測値のみ。5-8月キャッチアップ分は backfilled:true）
  - launchd `jp.finance.v4-forward` 平日19:30に bars→fins→台帳を単独実行
  - 判定 `verify_v4_forward.py`（日付ガード・verdict上書き不能・オーバーライド無し）:
    採用≥30・40bps後中央値>0・最大案件シェア<20%。供給不足もNO-GO扱い。

- 2026-08-08 **B案（cc1引け板寄せ集中×気配不要ML）を事前登録して判定 → NO-GO・「残る選択肢」表が全行決着**。
  v2凍結特徴量19本をret_cc_fwdデミーンに付け替え、D引け後計算→D+1引成建て→D+2引成返済
  （1日遅延・単元は前日終値・stale予測は真の翌セッションのみ適用）、0.5bps/side＋
  信用金利（買2.8%/売4.2%・245日割り）、本番制約。選択窓2018-2024:
  **net Sh0.22・DD−68%・負の年3/5・上位5日シェア137%・top10除去でSh−0.30**。
  グロス4.25bps/日 vs 全込みコスト2.43bps/日＝事前登録の2倍基準未達（確認窓未開封）。
  診断用の遅延なし上界（実行不能）はさらに悪い（Sh−0.07・グロス1.83bps）＝
  **遅延が死因ではなくグロス不在**。R2の「gross 3-11bps/日」との矛盾はグロス過大側で解消。
  `scripts/experiment_quotefree_cc1.py` / `docs/PREREGISTER_quotefree_cc1.md`。
  **「気配を必要としない日本株の場中/cc1戦略」は構造的に全滅が確定**（A/B/C/D全行決着）。
  残る運用経路は封印フォワード3本（V4 2027-12-01・ゴトー 2027-08-06・気配不要ML v2 2027-08-02）のみ。

- 2026-08-08 **FX複数通貨L/Sグリッド（トラリピ型）→ NO-GO。数理・実測とも決着**
  （`scripts/experiment_fx_grid.py` / `docs/PREREGISTER_FX_GRID.md`・7ペア分足・
  実測ハーフスプレッド・スワップはブローカー最良側 passthrough1.0/haircut0）。
  - **数理の恒等式を先に固定**: グリッドの連続極限は p=−k(P−A) で、
    PnL_gross=(k/2)[実現分散−終端乖離²]＝**マルチンゲールなら期待値0**。
    間隔・キャップ・通貨数は分布を変えるが期待値を作らない。源泉は**VR<1（平均回帰）のみ**。
  - **VR診断（選択窓2012-2019・7ペア中央値）**: 1h→1d 0.981 / 1d→1w 0.966 / 1d→1m 0.886
    ＝**弱い平均回帰は実在する**が、グリッドが収穫すると**グロス+0.29%/年**（フル在庫レバ1倍比）
    にしかならない規模。USDJPYだけVR≥1（トレンド体質＝トラリピ2022年破綻ペアと整合）。
  - **シミュレーション（真のヒステリシス実装・自己検証4項通過）**: 主仕様 Sharpe **0.163**・
    年率+0.66%・DD−7.9%・**上位5日シェア111%・top10除去でSh−0.16**・負の年3/8。
    感度も全滅（Δ2倍 0.246 / コスト半減 0.201 / **キャリー整合片側は−0.04でグロス自体が悪化**
    ＝キャリー正側のディップは下げ続ける。carry-dip NO-GOの再現）。確認窓未開封。
  - **「勝率は高いのに期待値ゼロ」を計測器レベルで実証**: マルチンゲール合成パスで
    シム勝率69%・平均0（worst−4.3% vs best+0.7%）＝グリッドの「稼げて見える」性質は
    分布の非対称であって期待値ではない。
  - ★**計測器の罠（修正済み・退行テスト4本）**: 終値クロス検出＋水準価格約定は
    「逆行継続に条件付け」る**悲観バイアス（−2.7SE）**。指値はタッチで約定するので
    高安タッチ検出＋OHLC単調3レグパスが正しい。**グリッド系のBTは約定検出の条件付けを必ず自己検証すること**。

- 2026-08-08 **FX追加5セル（セッション効果＋残余3セル）→ 全滅。G8公開価格データの探索はこれで枯れた**
  （`scripts/experiment_fx_session.py` / `experiment_fx_micro3.py`・累計20仮説）:

  | セル | 選択窓Sh | 死因・副次発見 |
  |---|---:|---|
  | Ranaldoセッション効果(7レッグ) | **−1.15** | ★**JPYレッグはグロスから負（−1.34bps/日）**＝東京セッション通し保有は仲値後反落が朝のフローを食う（ゴトー実測と整合）。午前3時間集中の感度は−2.30とさらに悪化 |
  | 同・EUR/USDのW形（診断） | 0.23/0.33 | **欧州時間下げ・NY時間上げは実在**（グロス+1.7bps/日>コスト1.1-1.2）だがSh0.3止まり。レッグ昇格は禁止済み |
  | 週末ギャップ・フェード | 0.345→**訂正 −0.28** | ★初出の「ネット+5.0bps/回」は**実装バグ**（下記V2エントリ参照）。仕様どおりの月曜正午手仕舞いは−1.33bps/回 |
  | 水曜トリプルスワップ | −1.76 | **帰無仮説（完全調整）がほぼ成立**。対照日比+0.7bpsの残差はあるがコスト2-3bpsの下 |
  | 時系列・週次リバーサル | 0.063 | VR<1（AUD/NZD/CAD 0.80-0.86）は実在するが**規模が小さすぎて収穫不能**（+0.7bps/週/レッグ） |

  確認窓2020+は5セルとも未開封。多重性は事前登録に明記済み。
  **FXの総括**: 実在が確認できた構造は①仲値フロー（ゴトー封印中）②EUR/USDセッションW形
  ③日曜再開の流動性プレミアム（下記V2）④三角乖離⑤VR<1 の5つ。**全てコストか分散の壁の下**にあり、
  G8×公開価格×個人スプレッドで越えられたのはゴトーティック形(0.856)だけ。
  残る未検証は指標イベント時刻もの（NFP=決定論的日付で構成可能・FOMC/CPIは日付リスト要）
  だが低頻度のためユーザー方針により優先度低。**FXの新セルは新データ（イベント時刻・
  オプションIV・フロー）なしには開かない**。

- ★★2026-08-08 **週末ギャップV2（多通貨13銘柄×商品/株/日曜初動の条件付け）→ 恒久クローズ。
  ただし機構が完全に特定でき、既存バグ1件を訂正**
  （`scripts/experiment_fx_weekend_gap_v2.py` / `docs/PREREGISTER_FX_WEEKEND_GAP_V2.md`）。
  - ★**訂正: micro3 H1 の「ネット+5.0bps/回」は実装バグだった**。週アンカーのずれで
    「月曜12:00手仕舞い」が実際には**翌日曜までの約1週間保有**になっていた
    （V2実装で同じ罠を踏んで発覚・micro3を修正して再計測済み）。
    仕様どおりのH1は **−1.33bps/回・Sh−0.28**＝もともとネット負。
    ※このバグはV2の`weekend_snaps`と同型（「翌週月曜のバーは翌週グループに属する」）。
    **週跨ぎスナップショットのgroupby(week)は必ずアンカーシフトを検査すること**。
  - **機構の特定（時間分解・2,741トレード・mid→mid）**:

    | 区間 | フェード方向グロス | t |
    |---|---:|---:|
    | 日曜17:00→18:00 ET（再開1時間） | **+5.20bps** | **+24.7** |
    | 18:00→月曜12:00 | −0.73bps | −0.7 |

    9年全てプラス（+1.3〜+7.3bps）。**プレミアムは再開後1時間で100%消尽**。
    一方コストは 再開hs 3.85bps＋18:00 hs 2.41bps＝**往復6.3bps > グロス5.2bps**。
    ＝**日曜再開の流動性供給プレミアムはスプレッドそのもの**であり、テイカーは
    定義的に収穫できない。JP寄付き板寄せ（60秒で69%消失・クロス内でしか受け取れない）と
    同一構造＝**流動性供給者の壁の7例目**。
  - **条件付けは原理的に不可能**: 商品/株指数（CME系）の日曜再開は**18:00 ET**で、
    プロキシが観測可能になった時点でプレミアムは消えている。金/原油/SPXギャップ・
    日曜初動・金曜整合・サイズの全セル（13銘柄×約15セル）が18:00エントリーで
    ネット負（全銘柄マイナス・凍結規則通過ゼロ）。**事前登録どおり族を恒久クローズ**。
  - 収穫の唯一の理論経路は**メイカー側**（再開の広スプレッド内に指値を置く側）だが、
    JP実測「メイカー転換はハーフスプレッドの17.5-22%しか残らない」（逆選択）が
    同じ構造に適用され、かつ日曜17時の指値運用は無人インフラが必要。追わない。
  - ★2026-08-08 追補: **分足での時間内分解（ユーザー質問「もっと短い時間足なら？」への実測回答）**
    （`scripts/analyze_weekend_reopen_decay.py`・直接7ペア1,665発火・出口は月曜正午固定）:
    ①**プレミアムは前傾**（0-2分1.02 / 2-5分0.90 / 5-10分0.99 / 10-15分0.42bps＝
    15分で約3.3bps・以降ほぼゼロ）に対し**スプレッドはほぼ締まらない**
    （エントリーhs 3.01→15分後2.64bps）＝減衰がスプレッド収縮に**全区間で勝つ**。
    後乗りポケットは存在しない（ネットは t+0 −1.28 → t+5 −3.13 → t+20 −4.33bpsと単調悪化。
    **テイカーの最良は「最初の気配で入る」でありそれでも負**）。
    ②スプレッド条件付けもダメ: **グロスとエントリーhsは無相関（+0.004）**だが、
    細スプレッド日曜だけ取る PIT セルは hs≤1.5bps で n=54（6回/年）・+8.2bps/回・
    t=1.47・Sh0.49＝ノイズかつ低頻度。**短い足でも閉鎖の結論は不変**（3方向目の確認）。

- 2026-08-08 **投資部門別売買状況（機関投資家のあしあと・市場レベル）→ NO-GO。
  「外国人買い越しについていけ」は週次集計レベルでは死んでいる**
  （`scripts/experiment_jp_investor_flow.py` / `docs/PREREGISTER_JP_INVESTOR_FLOW.md`・
  `/v2/equities/investor-types` を2008年まで遡って新規収集=18年971週・初使用）。
  - 主仕様（海外4週ネット>0でロング/フラット・PubDate翌営業日執行・2bps/side）:
    選択窓2009-2019 **Sh0.146 vs B&H0.419**＝完敗。L/S版−0.30・13週版0.23も全滅。
  - **診断IC表が決定的**: 全12部門の週次ネット(52週z)→翌週TOPIXのICは**全部門|t|≤1.35**。
    価格コントロール後の偏ICも同様＝**集計フローに前方予測力は存在しない**。
    フローは同時相関のみ（海外+0.43=順張り・個人−0.53=逆張りの定型は確認）。
  - 「市場レベル変数は効かない」「タイミングはB&Hに勝てない」の両既知則の3例目。
    **機関のあしあとは市場集計では死に、銘柄イベント粒度（下記）に賭けるのが正しい**。
- 2026-08-08 **大量保有報告書（5%ルール）新規イベント研究 → NO-GO。「5%取得についていく」は
  現代日本では負のドリフト**（`scripts/experiment_jp_large_holdings.py` /
  `docs/PREREGISTER_JP_LARGE_HOLDINGS.md`・メタデータ63,695件を新規収集）。
  - データ: `collect_edinet_large_holdings.py` → `data/jp_large_holdings/`（月別jsonl）。
    ★**EDINETの大量保有の縦覧期間は5年でローリング消滅**（2021-06は既に0件）＝
    遅れるほど歴史が消えるため**日次追記をデイリージョブに組込済み**（run_v4_forward_daily.sh）。
    銘柄解決は secCode＋EDINETコードリスト（`edinet_code_list.csv`）の2段。
  - 新規報告8,807件→実取引条件（¥1億フロア・重複除去・連続セッション）4,419件。
    選択窓2021-09〜2024-12の2,778件で判定:
    **市場超過中央値 −1.64%／平均 −1.07%／勝率42.4%／ES(5%) −33.1%**＝
    超過合計が負で集中基準は計算不能なレベル。事前登録4基準中3つ不合格。確認窓未開封。
  - 解釈: 新規5%報告の大半はパッシブ・事業法人・創業者で、報告時点は**短期的な買い圧力の
    出口**（取得完了の開示＝フローの終わり）。古典的な「アクティビスト追随」をやるなら
    **提出者名簿を先に凍結して新窓で別登録**が必要（事前登録の禁止事項に明記済み）。
  - CORPORATE_EVENTS ②支配変化ブランチはこれで初検証・棄却。残る生存はV4フォワードのみ。

- ★★2026-08-11 **medium_residual実験の財務特徴量8本は100% NaNだった＝「中期・個別残差ML」の
  NO-GOは実は「財務なし（価格8+需給13のみ）」の判定だった**（実証: `build_dataset()` の
  fn_* 非null率 0.0%・73.9万行）。原因はsymbol桁ずれの無言ゼロマージ（パネル5桁 vs fins4桁・
  **既知の罠の3例目**。SimpleImputerが全NaN列を無言で吸収したため検出されなかった）。
  - 記録の訂正: 「実際に売れる大型株集合ではグロスαが無い」は**財務抜きモデル**への判定。
    財務入りの中期残差MLは**未検証のまま**（再検証するなら判定窓2024+が消費済みである点に
    注意して新窓設計が必要）。
  - ★教訓: **疎な特徴量をimputerに通すパイプラインは、必ずマージ直後に非null率をassertする**
    （imputerは全NaN列を握り潰す）。ownership(2026-08-01)・flow_fund_sector_on(2026-08-11)と
    合わせ、fins/EDINET系のマージは全て sym4 で行い coverage assert を必須とする。

- 2026-08-11 **フロー×ファンダ・セクター内L/S・オーバーナイト（ユーザー指示）→ 全4セルNO-GO。
  3レンズの敵対的検証（陽性対照つき）で判定確定・前提の誤りも発見**
  （`scripts/experiment_flow_fund_sector_on.py` / `docs/PREREGISTER_FLOW_FUND_SECTOR_ON.md` /
  検証3エージェント・判定はいずれも verdict_reliable=true）:

  | セル | グロスbps/日 | 全込みコスト | net Sh |
  |---|---:|---:|---:|
  | A: zsec(flow) 単独 | **+1.29** | 3.43 | −0.93 |
  | B: flow×B/P 50/50 | −0.11 | 3.41 | −1.56 |
  | B2: flow×B/P×ROE | +1.26 | 3.42 | −0.93 |
  | C: ダブルソート | +0.04 | 3.42 | −1.46 |

  - **死因の帰属（実測）**: ロング脚+5.65bps/晩 ≒ 市場夜間ドリフト+5.12（R7）＝選択αなし。
    ショート脚−3.08bps/晩＝ドリフトに逆行。**コストゼロでもグロスSh0.56**＝コストの問題ではない。
  - ★**借株の壁の「逆」現象（8例目にして初の反転）**: 制約を外すとグロスが**悪化**
    （1.29→0.87→0.16）。非貸借銘柄は夜間さらに続伸する＝夜間はショート自体が構造的に不利。
  - ★**バリューの追加は夜間グロスを実質的に毀損**（Bの劣化の2/3は合成そのもの・1/3がNaN交差。
    flowテールとB/Pテールの重なり21%でtop-8選択が薄まる）。
  - 検証の陽性対照: 同一パイプラインで日中フローIC+0.0129/t=7.77を完全再現＝計測系健全。
    コスト勘定（往復2.0+金利1.43bps）・金利の二重引きなし・8/側充足76%も検算済み。
  - 軽微な事前登録逸脱2件を記録: B/P分母と単元サイズにD終値を使用（規定はD−1・影響ゼロ近傍・
    有利側なのでNO-GOを強化するのみ）。
  - **教訓**: 陽性対照（既知のαが同一パイプで再現するか）を検証に必ず含める。
    否定的結論も敵対的検証してから確定する（本日はバグ3件の日だったが、この判定は本物）。

- 2026-08-11 **保有期間フロンティア（ファンダ×フロー×h∈{1,5,20,60}・時間軸自由化のユーザー指示）
  → 全12セルNO-GO。「正しくマージした財務の断面αは保有期間に依らず不在」が確定**
  （`scripts/experiment_fund_horizon_frontier.py` / `docs/PREREGISTER_FUND_HORIZON_FRONTIER.md`・
  hトランシェ重畳・実測回転課金・ショート金利4.2%/245日・貸借∧非規制ショート・選択窓2018-2024）:

  | シグナル | h=5 | h=20 | h=60 | 備考（グロスbps/日） |
  |---|---:|---:|---:|---|
  | V: zsec(B/P) | −0.24 | −0.14 | −0.22 | グロス+0.10〜+0.40 |
  | Q: zsec(ROE) | −0.38 | −0.34 | −0.39 | **グロスから負** |
  | VQ | −0.12 | −0.22 | −0.35 | 最良グロス+0.74（h5） |
  | F: flow | h1 −0.96 / h5 −1.19 | | | cc脚は負（日中専用の再確認） |

  - ★**構造的な発見: 中長期L/Sの拘束コストは売買でなくショート金利**。回転コストはhで
    償却できる（h60で回転0.015/日）が、**金利0.85bps/日（半分ショートの4.2%年率）は
    保有を伸ばしても消えない床**。全込みコストの下限≈0.9bps/日に対しグロス最良0.74＝
    どのhでも届かない。**「保有を伸ばしてコストを薄める」戦略族はこの床の上でしか成立しない**。
  - 確認窓2025+は未開封。ファンダ系の残る生存はイベント駆動（V4フォワード）のみ。
- 2026-08-11 **medium_residual の財務マージ修正版を凍結仕様のまま再判定 → NO-GO のまま
  （むしろ悪化）・訂正が完結**。財務8本を実際にマージ（カバレッジ38.7%→97%・
  BPS等は開示済み値の前方保持=value_event_modelと同一のPIT解釈）した結果、
  OOS24+ グロス Sh **−0.07**（財務NaN版は+0.19）・2025 −0.68・2026 −0.84。
  ＝**財務を正しく入れると中期残差MLはグロスから負**。元のNO-GO判定は正しかったが、
  正しい帰属は「大型株集合では価格+需給にも財務にも中期断面αが無い」。
  フロンティア実験（上記）と整合。財務入り中期MLの再検証は不要。

- 2026-08-11 **ロングオンリー・フロンティア（現物・普通形・ユーザー指示）→ 全9セルNO-GO。
  ただしバリューロングの超過は断面系で過去最良＝「実在するが1レジームの塊」**
  （`scripts/experiment_long_only_frontier.py` / `docs/PREREGISTER_LONG_ONLY_FRONTIER.md`・
  セクター内z上位10%等ウェイト・hトランシェ・現物板寄せ0.5bps/side・判定は等加重市場超過と
  TOPIX B&H比で凍結・選択窓2018-2024）:

  | セル | net Sh | 超過IR | 超過年率 | 超過top5シェア | top10除去IR |
  |---|---:|---:|---:|---:|---:|
  | **V_h20（最良）** | 0.44 | **0.52** | **+4.83%** | **44%** | 0.14 |
  | V_h5/h60 | 0.35/0.41 | 0.48/0.46 | +4.5/+4.2% | 47/50% | 0.11/0.07 |
  | Q系（ROE） | 0.21-0.30 | 0.20-0.28 | +1.3-2.0% | 73-117% | 負〜0 |
  | VQ系 | 0.30-0.37 | 0.42-0.50 | +2.5-3.2% | 43-56% | 0.03-0.14 |

  - **落ちた基準は2つ**: ①total Sharpe 0.44 < B&H+0.2=0.62（小型バリュー箱のボラが高い）
    ②**超過の44%が上位5日**・top10除去でIR 0.52→0.14に崩壊。
  - ★**診断で集中日の正体を特定**: 上位10日は**全て2020-04〜2021-03**（最大は
    2020-11-09ワクチン発表の+511bps）＝**COVIDバリューローテーション1回の塊**。
    残りの日々の超過は+1.4bps/日の細流（暴落日だけ+11.7bps=防御性）。
    ＝バリューロングの超過は「日次αの積み上げ」でなく「数年に1度の回転を待つ因子エクスポージャ」。
    集中基準はこれを正しく検出した（決算前ドリフト・増配イベントと同じ署名）。
  - screener既知の月次バリュー確認窓Sh0.72とも整合。**普通のロング断面はこれで決着**。
    ファンダの生存はイベント駆動（V4）のみ、が改めて確定。確認窓2025+未開封。

## ★★★結論: 気配前提は棄却され、ensemble_core は執行不能（2026-07-31 ユーザー実測）

**ユーザーによる実測の結論: 「気配値と寄り付き値の間に有効な関係はない。予測も収束もしない。
気配値を扱おうとするのは無駄」。** 事前登録した判定（建玉一致率 <50% → NO-GO）に照らし
**実弾NO-GO が確定**。これは vendor や実装の問題ではなく市場構造の問題なので、
**現行kabu API／逐次取得経路では再挑戦不要**。FLEX Standard/MBOの全板・注文イベントまで
使った場合の到達可能性は未判定だが、2026-08-01のユーザー判断でこの救済研究は実施しない。

### なぜ「戦略が死ぬ」まで言えるのか（論理の鎖・各リンクは既に実証済み）

1. **αは当日の寄付きギャップにしか無い**（R1）: 当日寄値を外すとgross年率は最良+4.9%、
   一方 ensemble_core は +69.4%。場中フラットαは実質すべてオーバーナイトギャップに宿る
2. **そのαは板寄せクロスの中でしか受け取れない**（R11）: 09:00 21.4bps → 09:01 6.7bps
   （**60秒で69%消失**）→ 09:05 3.4bps。情報系αでは説明できない崖＝流動性供給プレミアム
3. **遅延エントリーでは代替できない**（R11）: 9:0X建ては全変種NO-GO（最良 net Sh −0.12。
   **エントリーコストを0にしても理論上限1.59** vs 板寄せ3.29）。パッシブ指値も逆選択で相殺
4. **クロスに参加するには、クロスが確定する前に発注する必要がある** ＝ 当日ギャップの
   事前推定が必須 ＝ **寄前気配が必須**
5. **その気配が寄値を予測しない**（本項）

→ 1〜5 は閉じた輪。**αは実在するが、¥20M個人信用口座からは到達不能**。

### 「データ源を変えれば直る」ではない理由

気配が予測しないのは**特別気配の機構そのもの**による。表示値は更新値幅（制限値幅/10。
¥2,000-3,000帯で1step=¥50=205bps）に縛られた**規制上の段階値**であって需給の均衡値ではない。
さらに **αを生む銘柄ほど気配が機能しない**: |residual_gap|最上位五分位は09:00約定率が
わずか4.1%（SHORT 2.5%）なのに**αの54.4%**を生む。つまり「気配が当たる銘柄」と
「αがある銘柄」がほぼ排他。岡三RSS（特別気配種別まで取得可）や立花APIに替えても、
より正確に「規制上の段階値」が取れるだけで、この排他性は解消しない。
旧結論ではFLEX Historical購入も不要としていたが、これは情報集合I2/I3でも同じと未検証のまま
一般化していたため撤回する。ただし、撤回は研究再開を意味せず、ユーザー判断により購入・検証しない。

### 有効なままの知見（ここは無効化されない）

- リサーチ層の成績（理想BT・単元BT）は**シミュレーションとしては引き続き正しい**。
  死んだのは「シグナルを実時間で入手する経路」だけで、α自体の存在は否定されていない
- 板寄せコストの実測（寄り1.5/引け0.5bps）・αの正体（クロスの流動性供給プレミアム）・
  データバグ修正（schema v7/v8・調整基準スプライス・借株フィルタ）は全て有効
- 約40ファミリーの棄却理由も有効（ただし**14bps前提の棄却は低コスト前提で再評価の余地**）

### 残る選択肢（すべて「気配を必要としない」もの）

| 案 | 気配不要の理由 | 現状の評価 |
|---|---|---|
| A. 前日終値ベースの寄成→引成 | 前日確定データのみで発注 | **棄却済み**（gross年率 最良+4.9%・ML4変種は全net負） |
| B. 引成→翌引成（cc1・引け板寄せ集中） | 前日終値で当日15:30前に発注 | **棄却済み（2026-08-08）**: 0.5bps/side＋信用金利でも net Sh0.22・グロス4.25bps vs 全込みコスト2.43bps＝2倍基準未達。R2との矛盾はグロス過大側で解消 |
| C. 資本増・別口座で執行チャネルを変える | — | 板寄せ参加は口座を変えても同じ機構。**解にならない** |
| D. 研究の停止／別アセットへ | — | 実弾を諦める判断 |

**全行決着（2026-08-08にBを判定して完了）**。気配を必要としない日本株の場中/cc1系の探索はこの表で閉じた。

### ★kabu APIのスループットが律速（2026-07-31・「APIが遅すぎる」の定量化）

ユーザー指摘「APIが遅すぎます」を数値化した。kabu の /board は**1銘柄1リクエスト**で
参照系10件/秒＋50銘柄ごとに `/unregister/all` が必要 → **実測 ~9.5銘柄/秒**。
¥10億フロアの467銘柄で**約49-66秒**かかる。つまり最初に読んだ銘柄と最後の銘柄で
**気配が1分ずれる**＝各銘柄をバラバラの時点で観測した横断面になる。
寄前の最終数分は気配が最も速く動くので、**この時間ずれはランダム誤差として振る舞う**
＝事前登録した対応表で**唯一の致命ケース**（特別気配そのもののクリップ型歪みは維持率81%）。

**ユニバースを削って高速化できるか** → 動作点が無い（本番条件・OOS24+・7bps）:

| 流動性フロア | 日次銘柄数 | 板取得 | 年率 | Sharpe |
|---|---|---|---|---|
| ¥10億 | 467 | 49秒 | 83.8% | **3.04** |
| ¥20億 | 293 | 31秒 | 37.3% | 1.51 |
| ¥50億 | 136 | 14秒 | −5.4% | −0.27 |
| ¥100億 | 64 | 7秒 | 0.6% | 0.03 |
| ¥200億 | 24 | 3秒 | 1.8% | 0.12 |

**αは流動性の下位テールに宿る**ため、スナップショットが間に合う規模（≤100銘柄）まで
削るとαが消える。**kabu のスループットでは原理的に成立しない**。

**必要スペック（他ブローカー検討の判定基準）: 467銘柄を≤5秒＝約95銘柄/秒（kabuの約10倍）**。
候補は2つだけで、どちらも**寄前気配が取れるかは未実測**:
- **立花証券e支店API**: 時価一括**120銘柄/リクエスト** → 467銘柄が4リクエスト。無料・
  純HTTP/JSON・Linux可。**要実測: 寄前気配(特別気配含む)が時価一括に載るか**
- **楽天MS2 RSS**: 市況**500銘柄**・Excelへのpush配信なのでポーリング遅延が原理的に無い
  （467銘柄が丸ごと収まる）。Windows+Excel常駐が必要
- 岡三RSSは寄前/特別気配種別まで明記だが**50銘柄上限**で不足。

**重要な含意**: 「気配が寄値を予測しない」という実測は**66秒スメアと分離できていない**。
kabuでの再測定は無意味（同じスメアが乗る）。上記いずれかで同時性のあるスナップショットを
取れて初めて、気配前提の可否が本当に判定できる。

## ★★特別気配の現実（2026-07-30・ユーザー指摘を受けた再検証・最重要）
**ユーザー指摘**「気配と寄値が¥50違うのはザラ・しばらく寄りつかないのもザラ」は**完全に正しく、
検証の一部は誤っていた**。全市場1分足で再計算した結果:
- **選定銘柄の41.7%が09:00に約定していない**（LONG 25.5% / **SHORT 58.0%**・遅延p50 6分）。
  初回約定時刻は**3の倍数に鋭い山**（0分88%/3分3.7%/6分1.1%…）＝特別気配の更新間隔そのもの。
  iidノイズでは絶対に出ない離散構造で、ユーザーの理解が正しいことをデータが直接示す。
- **αは特別気配銘柄そのもの**: |residual_gap|最上位五分位（|gap|中央9.0%）は
  **09:00約定率わずか4.1%（SHORT 2.5%）なのにαの54.4%**を生む。
  ＝「特別気配を避ける」は「αを捨てる」と同義。
- **除外運用は成立しない**: 09:00非約定を除外すると Sh 2.72→1.94・年率75.6%→25.0%、
  さらに**元本比29.9%の買い越しに変質**（ドル中立でなくなる）。繰上補充は更に悪い(0.94)。
- **★R11の「+10分以降はα寄与−2.5%」は誤り**（1,022銘柄subsetのバイアス。この subset は
  グロスP&Lの52%を取りこぼしていた）。全市場で正しくは**+10分以降がα寄与26.1%・
  68.6グロスbps＝最も厚いバケット**。STATUS/READMEのR11記述も訂正。
- **執行そのものは無問題**: 遅れて寄っても板寄せクロスなので、日次バーの始値
  （＝その日の初回約定価格）で建てる現行BTは**正しい**（1分足で再現し日次net相関1.000000）。
  「前場に一度も寄らない」は建玉の1.16%のみ＝**寄成失効リスクは実質ゼロ**。
- **危険なのは執行でなく銘柄選択**: 08:50気配の系統誤差は機構的下限で
  **エクスポ加重602bps・α加重1,060bps**（更新値幅=制限値幅/10。¥2,000-3,000帯は
  1step=¥50=205bps＝ユーザーの言う「¥50」はまさに1ステップ）。
  **既存の「許容σ25bps・限界51bps」は桁が2つ足りず撤回**。
- **ただし誤差の"質"で結論が変わる**（歪みモデル別の実測）:
  ①**一様圧縮λ**（真のギャップ×λ）は**情報損失でなくキャリブレーション問題**
   （λ=0.3でも順位相関1.000・λ既知の補正でSharpe100%復元）
  ②**上限クリップ**（±3%で頭打ち＝特別気配の本質に近い）は Sharpe 1.85(68%)まで低下するが
   **床が1.8-2.0で下げ止まる**（svdnスリーブは死ぬがMLは生存）
  ③**状態依存ノイズ**（誤差=ギャップの50%）は 2.53(93%)と最も頑健
  ④**致命的なのはランダム誤差のみ**: 全銘柄一律のσ250bpsでSharpe半減・310bpsで1.0・500bpsでゼロ。
   ただし**誤差が特別気配銘柄(全行の12.9%)に限定されるなら1step 2.51(92%)・1.5step 2.10(77%)**。
- **帰結**: 実弾可否はquotesnapの実測でしか決まらない。測るべきは「誤差の大きさ」ではなく
  **「誤差の系統成分（気配自体から予測可能な部分）とランダム成分の分解」**。
  系統成分が支配的なら補正して生存、ランダムが250bps級なら不成立。
  **合格基準「50bps以内」は機構的に達成不可能なので作り直す**。
- ★2026-07-31 **その計測器自体にバグがあったので修正**（`scripts/analyze_quotesnap.py`・
  `--selftest` で再現/検証可能。tests/test_quotesnap_decompose.py で退行防止）:
  旧版は「実ギャップ を 気配ギャップ に回帰」していた。ノイズを含む変数を説明変数に置くと
  誤差変数バイアスで傾きが Var(真)/(Var(真)+Var(誤差)) 倍に縮むため、
  **純ランダムσ250bps を λ=0.40 と報告** → 旧解釈文では「気配が過小表示＝圧縮＝補正可能・無害」。
  **最悪ケースを最も安全なケースとして報告する向きのバグ**だった（真の一様圧縮λ=0.3も
  λ=3.33＝逆数で表示され、解釈文と正反対）。正しい向きは「気配 を 実寄値 に回帰」
  （測定式 quote=λ·actual+ε の素直な推定）。修正後は既知の歪み5種で λ・σ_ε とも真値を回収。
- ★2026-07-31 **実弾GO/NO-GOの合格基準を事前登録した**（実測を見る前に確定。
  `scripts/quote_distortion_calibration.py`。既知の歪みを本番と同じ経路
  ＝ensemble_core ¥20M・8銘柄/側・信用2倍・¥10億フロア・7bps に注入して作った対応表）:

  | 歪み | 建玉一致率 | OOS24+ 年率 | Sharpe | 維持率 |
  |---|---|---|---|---|
  | なし（基準） | 100% | 83.8% | 3.04 | 100% |
  | 一様圧縮 λ=0.5 | 100% | 83.8% | 3.04 | **100%** |
  | 一様圧縮 λ=0.3 | 100% | 83.8% | 3.04 | **100%** |
  | ±3%クリップ | 78.6% | 57.8% | 2.45 | 81% |
  | ランダム σ=100bps | 53.7% | 56.2% | 2.21 | 73% |
  | ランダム σ=250bps | 36.6% | 32.6% | 1.40 | 46% |
  | ランダム σ=500bps | 30.2% | 18.9% | 0.83 | 27% |

  **判定手順**: `python scripts/analyze_quotesnap.py --book` で実測の建玉一致率を出し、
  この表に当てて Sharpe 維持率を読む。**≥75%→GO / 50-75%→減額(¥5M)で開始 / <50%→NO-GO**。
  - **一様圧縮は一致率100%＝λがいくら小さくても完全に無害**（3.037226 が基準と完全一致）。
    したがって**λ単独では絶対に判断しない**。効くのはランダム成分だけ。
  - **順位相関も使わない**（クリップは順位相関1.000でも一致率78.6%）。
  - ML行の留保: 学習側も歪ませているため誤差変数バイアスが保護的に働き**楽観側**。
    ランダム誤差の行は下限として読む（svdnスリーブにはこの留保は付かない）。
  - 検証済み: σ=250bpsを注入した偽quotesnapで --book が一致率32.9%を返し表と整合
    （表36.6%・10営業日サンプル差）。
- ★2026-07-31 **判定ツールが読むデータ自体のバグを2件修正**:
  ①`daily_adj_*.parquet` の**生値列 O/C は2022-2024に列自体が無く、2025も非NaNが3.2%**。
   `analyze_quotesnap`/`analyze_effective_cost` が `columns=["O","C"]` を決め打ちしており、
   **気配の3%だけを見て全体を判断**していた（実効コスト側は列が無い年でArrowInvalid）。
   → quotesnap側はパネル（調整基準解決済み）から取得、コスト側は調整値フォールバック
   （非NaN 3.2%→92.7%）。退行テスト `test_official_prices_are_not_a_3pct_subsample`。
  ②建玉一致率をスコアフレーム同士で測ると、svdn_concentrated が各フレームで自分の
   上位5%テールだけを残すため inner merge の時点で不一致が構造的に消える
   （2,046日中16本以上残るのは24日＝1.2%で測っていた）。**必ず約定ベース(blotter)で測る**。
- **判定に使う4つの量と、使ってはいけない量**:
  ①**λ**（圧縮率）… それ自体は無害。決定論的なら補正で100%復元
  ②**R²**（★判別子）… 高い=系統的で補正可能 / 低い=真のランダム成分。**λでなくR²で分ける**
  ③**σ_ε** … R²が低いときだけ意味を持つ（250bps級でSharpe半減）
  ④**選択一致率**（気配で選ぶ上位/下位8本 vs 実寄値で選ぶそれ）… **戦略に直結する唯一の量**
  ✗**順位相関は判定に使わない**: ±3%クリップは順位相関1.000なのに選択一致12%
  （テールが同値に潰れて上位k本が選べない）。戦略が見ているのはテールなので順位相関は無意味。

## ★αの正体の特定（2026-07-30 R11・戦略理解の中核）
- **ensemble_core のαは「情報」ではなく「板寄せクロスの流動性供給プレミアム」**だった。
  1分足の実測（選択窓2024-08〜2025-09・|w|加重・エントリー→引け）:
    09:00板寄せ **21.4bps** → 09:01 6.7bps（**60秒で69%消失**）→ 09:05 3.4bps（84%消失）
    → 09:15 1.5bps → 10:00 1.2bps（以降1.5-2bpsで横這い）
  情報系αならもっと滑らかに減衰するはず。この崖は「夜間の需給インバランスを単一約定価格で
  吸収する対価」であり、**クロスの中でしか受け取れない**。|gap|分位に比例してスケールする
  （最上位分位で3倍＝呼値バウンスではない）ことも裏付け。
- **帰結: 遅延エントリーは気配問題の解にならない**。9:0X建ては全変種NO-GO
  （最良 net Sh −0.12。**エントリーコストを0にしても理論上限1.59** vs 板寄せ3.29）。
  パッシブ指値もメイカー約定63%でコスト半減するが**逆選択でグロスが−0.19→−6.37bpsに悪化**し差引ゼロ。
- **意思決定への含意**: 気配が多少劣化しても板寄せ執行は圧倒的に有利
  （歪みモデル別の実測でも**床Sharpe1.8-2.0**＝一様圧縮なら100%復元・クリップでも1.85で、
  代替の最良 net Sh −0.12 を圧倒する。※旧「σ51bpsで−10%」を根拠に使わないこと＝撤回済み）。
  **気配を捨てる方向でなく、気配の実測精度を確認する方向（quotesnap）に投資すべき**。
- R11の他3ファミリーもNO-GO: 板寄せのみ執行（12:30後場寄り建ては gross −2.4bps・
  11:30前引けは薄すぎてコスト67.8bps/side）、修正コストでの棄却候補再評価（上位3件とも未達）、
  前場→後場（前場リターンの反転ICは−0.013〜−0.024でq0/q9の単調反転なし）。
- **副産物（要事前登録・未検証）**: 寄付きが+10分以降にずれる超大ギャップ銘柄
  （|residual_gap|中央11.2%・エクスポ10.3%）は板寄せ約定でも−4.2bpsでαを毀損。
  「推定gap>8%は建てない」上限フィルタの案。**選択窓の観測なので閾値を先に固定して
  確認窓で1回だけ検証すること**。

## 板寄せ執行コストの実測（2026-07-30・重要な前提訂正）
- 寄成/引成はオークション＝**単一約定価格**なのでスプレッドは原理的に0、一日信用で
  手数料/金利/貸株料も0 → **真のコストは自分の注文がclearing priceを動かす分（インパクト）だけ**。
- 参加率実測（¥20M本番ブロッター×1分足）: 寄り p50 0.55-0.85%/¥加重1.0-1.34%、
  引け p50 0.14-0.18%/¥加重0.44%。**引けの板寄せは寄りの3-5倍厚い**。
- 独立3系統（用量反応・Kyleλ・引け実測スリップ）が収束し、
  **寄り1.5bps/side・引け0.5bps/side（往復2.0・レンジ0.6-4.5）**。
  **現行の7bps/sideは3.5-7倍の過大**（OOS24+で Sh3.04→4.59・年率+43pt・+¥8.6M/年）。
  計画値は**対称2.0bps/side（往復4bps）**。R9の呼値1.21bps/sideはザラ場指値の話で板寄せに足さない。
- ★**コスト数値の使い分け（3つの数字が混在するので必ずここを見る）**:

  | 数字 | 何のコストか | 使う場面 |
  |---|---|---|
  | **1.5 / 0.5 bps/side**（往復2.0） | **板寄せ**（寄成・引成）の実効コスト推定 | ensemble_core本体。3系統収束の実測ベース |
  | **1.21 bps/side** | 現行呼値表・流動銘柄(¥10億)の**ハーフスプレッド** | ザラ場**指値**（メイカー）の理論下限 |
  | **6.66 bps/side** | ザラ場**テイカー**の実測（スプレッド+インパクト） | ザラ場執行を伴う戦略の損益分岐 |

  混同禁止: 板寄せに1.21を足さない（板寄せに気配スプレッドは存在しない）。
  R7の「呼値半スプレッド7.54bps」は**2014年細分化前の粗い呼値表由来で過大**＝現行表なら1.21〜6.66帯。
  ただし**R7の棄却は覆らない**（死因はコストでなく「残存グロスが|夜間|>5%の4.2%の建玉に集中し
  ±5%winsorizeで符号反転」＝ストップ高級の寄値で約定不能。コストを0にしても取れない）。
  **7bps/side を機械的に置換しない**（verify_baselineの7bpsは環境再現アンカー。
  正本差し替えはライブ実測40営業日の合格後）。
- **測れないものの明示**: fill vs 公式寄値/引値の乖離は板寄せ約定である限り構造的に0で、
  **コストの実測にはならない**（自分のインパクトは公式価格に内包されバックテストも同じ価格を使う）。
  監視は **参加率p90（寄り≤3%/引け≤1.5%）・数量約定率≥98%・|slip|≤0.5bps（板寄せ外約定の検知）**
  に置き換え済み（live/costs.py・analyze_effective_cost.py）。
- **一日信用プレミアム料（上限100bps/日）は執行コストと別建てで日次計上**。桁が違う。
- **キャパの理由が変わった**: 旧評価は資本を上げてもコスト7bps固定で¥200M Sh2.73としていたが、
  参加率∝資本でコストを比例させると ¥50M Sh3.55 / ¥100M Sh1.78 / ¥200M 負。
  **天井~¥100M・安全圏¥50M**（結論は同じだが理由は「50単元キャップ」でなく「参加率×インパクト」）。
- **verify_baseline の7bps期待値は据え置く**（環境再現アンカー）。正本差し替えはライブ実測
  40営業日の合格後。低コストを根拠にした既存棄却候補の再選抜は**選択行為**なので
  新しい未使用期間で判定式を事前固定して1回だけ。

## 取引時間とスキャルピング可否（2026-07-30 R9・一次情報＋実測）
- **現物**: 前場9:00-11:30/後場12:30-15:30、注文受付8:00-11:30・12:05-15:30、
  **15:25-15:30はプレクロージング（約定ゼロ・板寄せは15:30）**、現物にNCPは無い。
- **先物(OSE)**: 日中 プレオープン8:00-8:44→**寄板合せ8:45**→ザラバ→プレクロージング15:40-15:45→
  **引板合せ15:45**。ナイト 16:45-16:59→**17:00**→ザラバ→5:55-5:59→**翌6:00**。
  NCPあり。**17:00開始のナイトは「翌営業日」の取引日に属する**（2024-11-05の定義変更）。
  NK225FのLTDはSQ日(第2金)の前営業日・ロールはSQ週火曜に建玉50%クロス・
  **miniは出来高の95.5%が四半期限月**（月限で組むと死板を掴む）。
- **kabuステーション稼働は6:30〜翌6:15**（ナイトをほぼカバーするが**6:00前後でセッション切断と
  ナイト引け板寄せが重なる**＝ナイトを触るなら17:00-翌5:00に閉じる）。
- **実測の流動性分布**（1分足・1,022銘柄）: **15:30の板寄せ1分だけで日次売買代金の13.5%＝
  9:00寄付(4.0%)の3.4倍**。9:00-9:30で20.9%。11:30前引けは板寄せなのに通常分と同水準（0.15%）。
  → ensemble_coreの寄成→引成は最厚2点を突いており設計は妥当だが、**入口の板は出口の1/4**。
  **実効コストは寄り/引けを分離して記録すべき**。
- **スキャルピングは棄却・再挑戦不要**: 1分足でOOS 770セルを総当たりして**ネット正はゼロ**
  （最良でも−0.55bps）。最良セルのグロスは**0.295bps/往復** vs 往復コスト2.2-3.4bps＝8-11倍不足。
  lag=0の見かけの平均回帰は**93%がbid-askバウンス**。集中は逆効果（強シグナル銘柄ほど広スプレッド）。
  メイカー転換もハーフスプレッドの**17.5-22%しか残らない**（逆選択）うえkabuは訂正不可・OCOなし・
  PUSH400ms間引きで実装不能。必要IC(1分)0.373 vs 到達可能0.015＝25倍不足。
  **損益分岐ホライズンはIS当てはめ+楽観コストでも約60分**、現実には数時間〜日次。
  先物はコスト無害（ミニ往復0.924bps・マイクロ1.153）だが**α不在**（指数IS上限は1分0.15/15分0.61bps）。
  ＝**株はコストで負け、先物は競争でαが無くて負けの二正面**。
- **呼値コスト記載の訂正**: 従来の「ハーフスプレッド4.77bps/side」は**2014年細分化前の粗い呼値表**
  由来の誤り。現行表での流動銘柄(¥10億フロア)は**中央値1.21bps/side**（Roll実効1.39・
  ティック実測往復2.0-2.7bps）。**ただし本番前提の7bps/sideは板寄せ+クリップ込みで別物なので
  機械的に置換してはいけない**（板寄せ実効コストの実測が先）。
- 先物イントラデイは**J-Quantsにエンドポイント自体が無い**（403）。ただし
  **JPX DataCubeで日経225mini 1分足が個人購入可（¥220/1ヶ月分・2012年〜・5年で¥13,200）**。

## kabu API の先物対応（2026-07-30 一次情報で確認・戦略は未確定）
- **先物・オプションの発注に正式対応**（`POST /sendorder/future`・2020-10開放）。
  対応商品: 日経225ラージ/ミニ/**マイクロ**/TOPIX/ミニTOPIX/グロース250/JPX400/NYダウ/VI/
  Core30/REIT。板・時価も `/board`・`/symbol` で取得可（先物は exchange=2/23/24）。
- **銘柄コードは限月込みの9桁**（例 "165120019"）。`GET /symbolname/future?FutureCode=
  NK225micro&DerivMonth=0` で解決。**自動ロールは無く**、取引最終日に 0 を指定すると
  当日でSQを迎える限月が返るため、SQ週は自前で限月を切り替える必要がある。
- **最重要の制約: 先物に「寄成(13)」は存在しない**。FrontOrderType は
  120=成行 / 20=指値 / **18=引成(TimeInForce=FAKのみ)** / 28=引指(FAS) / 30=逆指値 の5種。
  現行 ensemble_core の「寄成→引成」のうち**引成は移植可能だが寄成は不可**。
  寄付き参加はプレ・オープニング中に成行/指値を投げる形になるが板寄せ参加の可否は
  公式に明記なし＝**実機検証が必要**。
- Exchange: 2=日通し(指値FASと逆指値FAKの2通りのみ＝極端に狭い) / **23=日中** / **24=夜間** /
  32,33,34=SOR。**SORでは引成・引指が使えない**（株式の「信用はSOR必須」とは逆方向の制約）
  → 引け手仕舞いを引成でやるなら Exchange=23。**マイクロはSOR対象外**。
  ナイトセッション発注可（Exchange=24・実績あり）。
- 口座: 証券総合口座とは別に**先物・オプション口座の開設申込＋WEB審査が必要**。
  kabuステーションAPI自体は Professional/Premium プランが前提（株式と共通・商品別の設定は無い）。
- コスト: 手数料(税込/枚) マイクロ11円・ミニ38.5円・ラージ275円・ミニTOPIX41.8円。
  証拠金はVaR方式（ミニ約45万/枚・マイクロ約5.5万/枚が目安・日次変動。実装は
  `/wallet/future` を正とする）。取引単位: ミニ=指数×100(呼値5円=¥500/tick)、マイクロ=×10。
  **¥20Mならマイクロ（1枚≈¥62万）が刻みとして適切**（ミニ1枚≈¥624万＝資金の31%で粗い）。
- **PUSH/REST の50銘柄枠は株式と共有**（現行の株式運用が45件ごとに/unregister/allする運用と
  枠を奪い合う）。レート制限も共通（**発注10件/秒**・情報10件/秒・PUSH400ms間引き）。
- 実装ギャップ: kabu_client.py は株式(信用)専用。send_future_open/close・限月コード解決・
  先物用FrontOrderType定数・Exchange体系の分岐が未実装（to_kabu_symbol は先物に通してはいけない）。
- **注意: R8の検証では先物の日次タイミング戦略は全4ファミリー不合格**。
  「APIで扱えること」は確認できたが**回すべき先物戦略はまだ無い**。

## 先物・イントラデイデータの入手手段（2026-07-30 調査・出典付き）
- **kabu API に歩み値APIが新設**（2026-07-10 Ver5.44.0.0）: `GET /timeandsales/{symbol}`。
  市場コード 2=日通し/23=日中/**24=夜間**で**先物のナイト込みティック**が取れる。
  **当日セッションの全件累積を毎回返す**ので、日中引け後と夜間引け後に各1回叩けば
  日次アーカイブ完成（日経225先物1銘柄で約8-9MB・2.85秒）。**過去分は取得不可**＝
  今日から始めれば貯まる。秒間2件制限。板（気配）は含まれない。
- **無料**: 225Labo（要無料会員登録）が日経225先物/mini/マイクロ・TOPIX先物/ミニTOPIXの
  **1〜60分足を2001年〜・ナイト込み・期近期先別**で配布。規約は個人利用前提。
  取引所公式ではないので、DataCubeを1-2ヶ月だけ買って**突合検証用の正解データ**にするとよい。
- **買い切り（有料の本命）**: Portara CQG が MJNK(mini OSE) 1分足 2006-07〜 **$220**、
  JNK(ラージ) 1999-02〜 $220、JTPX(TOPIX) $220。**DataCubeより安く6年長い**
  （ナイトセッション収録の確認が発注前に必要）。
- **JPX J-Quants DataCube**（個人購入可・自己使用枠）: 当初把握より広く、
  **先物の歩み値(ティック)が¥330/月分・日経225は1988年〜/miniは2006年〜**、
  1分足は¥220/月分（mini 2012年〜・年単位2007-2011は¥2,640/年）。
  日経225mini ティック5年で¥19,800、主要5商品の1分足5年で約¥61,000。
- **J-GATE オルタナティブデータ**（OSEの**全注文情報=板**）: ITCHバイナリ 2021-09〜
  スポット**¥4,000/日**（最低5営業日）+ 申請ごと手続き費¥50,000。ITCHヒストリカルCSVは
  2016-07〜2021-09で¥2,000/日。20営業日で約¥13万。**個人契約の可否は要問合せ**
  （jpx-alternativedata@jpx.co.jp・料金体系は法人前提だが法人限定の明記はない）。
- **FLEX Historical にデリバティブ版は無い**（東証現物専用と確定）。10本板ヒストリカルも
  東証株式のみ。日経NEEDS/QUICK/時事/ミンカブは個人向け価格の公開なし。
- **手元データで代替可能**: 個別株1分足から**合成TOPIX 1分足**を作れる
  （1306との相関: 1分0.855 / 5分0.925 / 30分0.961）。ETF(1305/1306/1321等)も手元に実在。
  日中30分〜数時間ホライズンの研究は**追加コストゼロで即着手可能**。
- IB証券: Osaka Exchange L1 非プロ ¥200/月でOSE先物フルカバー（TWS API履歴の可否は要検証）。

## デプロイ / 公私分離（重要）
- **個人トレード関連は aim-research 組織の GCP に一切置かない**（公私混同禁止）。
  Web管理画面は https://trade.a-tokyo.jp ＝ GCPプロジェクト `atokyo-trade`
  （yuta@a-tokyo.jp 個人）。DNS は Cloudflare（a-tokyo.jp ゾーン、cloudflared
  cert.pem.a-tokyo.backup 内のAPIトークンで操作可能）。
- Cloud Run のヘルスチェックは `/api/health`（`/healthz` はGFE予約パスで不達）。

## ブランチ / PR
- base ブランチは **main**。原則 PR 経由（直接 main に入れない）。
- 変更後は自分で検証（下記）を通してから完了とする。可逆なら自己マージ可。
- **本番稼働に関わる変更（実発注ボット等）はマージ/デプロイ前にユーザー確認。**

## 検証（必須）
```bash
PYTHONPATH=. python -m pytest tests/ -q      # 全テスト
PYTHONPATH=. python -m py_compile trading/jp_intraday/*.py
```
- `trading/jp_intraday` は**研究コード**。リファクタは原則 **数値挙動を変えない**こと。
  公開関数の出力をサンプルデータでスナップショットし、変更前後で一致を確認する
  （特性テスト）。意図的に数値を変える場合は理由を明記し個別に検証する。
- 実データのイントラデイ期間は現状 **2026-06-01〜07-24（約39営業日）と短い**。
  多特徴量モデルは過学習に注意（ホールドアウト/ウォークフォワードを厳守）。

## 実行例
```bash
# 価格ベースのイントラデイ戦略実験
PYTHONPATH=. python scripts/run_jp_strategy_experiments.py
# 需給(OFI)戦略実験
PYTHONPATH=. python scripts/run_jp_tick_experiments.py
# ウォークフォワード（ridge モデル）
PYTHONPATH=. python -m trading.jp_intraday.research <bars> <memberships> <shares>
```

## 言語
応答・コミット・PR は日本語。
